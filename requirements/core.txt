# Базовый контур: сервис + лексический поиск. Без модели, без сети, без индекса.
# Этого достаточно, чтобы поднять HEFEST и пройти офлайн-сьют.
fastapi>=0.110
uvicorn[standard]>=0.53.0
pydantic>=2.13.5
numpy>=1.26
scikit-learn>=1.4
rank-bm25>=0.2.2
httpx>=0.28.1
segno>=1.6.6          # QR для этикеток/карточек, чистый python
