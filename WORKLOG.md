# Рабочий журнал пилота (SDS Safety RAG)

Хронология работ: что делали, почему так, какие цифры получили. Ведётся для питча/аудита
и передачи контекста. Свежие записи — сверху. Метрики — измеренные, не прогнозные.

---

## 2026-06-13 — Семантизация на полной базе, гибрид, сервис, генерация, реранкер

Контур: локальная Ollama на M1 Max (Metal-GPU), модели `bge-m3` (эмбеддинги) и
`qwen2.5:7b` (генерация). Полная база — 2601 вещество / 26010 чанков (`data/corpus_full.json`).

### 1. Семантическая переиндексация полной базы
- Запустили `build_semantic_index.py` → `data/embeddings.npy` (26010 × 1024, float32).
- **Оптимизация билда:** исходный цикл слал эмбеддинги по одному (`/api/embeddings`),
  6 эмб/с, ETA ~71 мин. Переписали на батчевый `/api/embed` (массив текстов за вызов)
  → **15 эмб/с** (×2.5), ~27 мин. Узкое место не GPU (Metal загружен на 100%, проверено
  `ollama ps`), а сериализованные HTTP-вызовы; батч убрал накладные. Чекпойнт раз в 1600
  векторов (частое сохранение всего .npy тормозило хвост — O(n) на батч).
- **Нюанс эндпоинтов:** `/api/embed` возвращает нормированные вектора (|v|=1),
  старый `/api/embeddings` — сырые (|v|≈25). Направление идентично (cos=1.000000),
  а eval всё равно нормирует по строкам — на косинус не влияет.

### 2. Eval семантики vs лексики (полная база, 28 запросов)
| Метрика | Лексика @2601 | Голый косинус | Семантика+плант-скоуп |
|---|---|---|---|
| Вещество top-1 | 60% | 84% | **84%** |
| Вещество top-3 | 84% | 96% | **96%** |
| Вещество+раздел top-1 | 52% | 64% | **64%** |
| Плант-скоуп top-1 | (100% на 48) | 33% | **100%** |
| Latency (median, вкл. Ollama) | 172 мс | 181 мс | ~210 мс |

- **Главное:** прогноз README подтвердился — на тысячах веществ лексика просела (top-1 60%),
  семантика вернула **84%**. Это обоснование прод-пути bge-m3 + вектора.
- **Находка про слои лексического гибрида (не масштабируются на 2601):**
  - *Плант-скоуп* — масштабируется, чинит 33%→100% **без потери предметного top-1**
    (срабатывает только на плант-запросах). Чистый выигрыш.
  - *Сущностный буст* — шумит: 5-буквенные стеммы коллизируют («кисло» от «кислот»
    в названии «аллохол» ловит запросное «кислоту» → «серная кислота» уезжает в аллохол).
  - *Интент раздела* — шумит: +0.12 ко ВСЕМ чанкам нужного раздела по 2601 веществу
    промотирует правильный раздел НЕправильного вещества (top-1 падал до 68%).
  - Вывод: коэффициенты, настроенные под 48 чистых веществ, нельзя переносить на 2601 как есть.
    Рабочая конфигурация на масштабе = **семантическая база + только плант-скоуп** (конфиг C).
- Файлы: `semantic_eval.py` (голый косинус), `semantic_hybrid_eval.py` (конфиг C, env
  `W_ENTITY/W_INTENT/W_SCOPE_IN` для экспериментов).

### 3. Подключение семантики к сервису
- `retriever.py`: класс `SemanticRetriever` (косинус по `embeddings.npy` + плант-скоуп),
  тот же интерфейс `.query(q, k, scope_substances)`, что у `HybridRetriever`.
  Тяжёлую лексическую инициализацию (TF-IDF/BM25) сделали опциональной (`lexical=False`).
- `service.py`: выбор ретривера по env `RETRIEVER=semantic|lexical`, честный фолбэк на
  лексику если `embeddings.npy` нет (сервис не падает). `/health` отдаёт активный retriever.
- Проверено end-to-end: `/search` отдаёт цианиды; `/ask` с `plant=Корунд` берёт в источники
  цианид калия / винилхлорид / HCN (скоуп работает, больше не «корунд белый»-абразив).

### 4. Grounded-генерация (qwen2.5)
- `/ask` с `LLM_BACKEND=ollama` собирает ответ на qwen2.5:7b строго по найденным разделам,
  цитаты [N], **temperature 0** (детерминированно — safety: цена ошибки = здоровье).
- `backends.py`: добавлен guard на пустой контекст + temperature 0.
- Проверено end-to-end (3 кейса):
  1. В базе («антидот при цианиде») → амилнитрит/тиосульфат/гидроксокобаламин + ссылка Р4.
  2. Плант-скоуп («завод Корунд») → список опасных веществ с классами и [1][2][3].
  3. **Вне базы** («сколько стоит билет на самолёт») → retrieval вернул 3 нерелевантных
     чанка, но модель честно ответила «в базе нет данных». **Grounding-guard держит,
     галлюцинаций нет** — ключевой safety-аргумент, теперь измеренный.

### 5. Реранкер (два бэкенда: кросс-энкодер и LLM)
Цель: дотянуть правильное вещество из top-3 в top-1 (пограничные запросы; часть промахов —
дубль-записи ингеста: «азотная кислота+», «б) контроль по формальдегиду»).
Факт: на машине **torch 2.8.0 + MPS** уже есть (README устарел про «не тянет torch»).
Ollama `/api/rerank` нет (404). `sentence-transformers` ломается на torchcodec→ffmpeg (нет libavutil).

**Конвейер (общий):** retrieve top-N косинусом(+скоуп) → реранк → top-k. Два сменных бэкенда
с одинаковым интерфейсом `.rerank(query, candidates, top_k)` (`SemanticRetriever(reranker=...)`):

- **(b) LLM-реранк на Ollama** (`reranker_llm.py`, qwen2.5, listwise, temp 0) — **измерено:**
  top-1 84%→**88%**, вещество+раздел 64%→**68%**, плант-скоуп 100%. НО latency **~6.2 с/запрос**
  (генерация на каждый запрос) и top-3 96%→92% (реранк выкинул один правильный из тройки).
  Вывод: для прод-`/search` слишком медленно; ценность — air-gapped (без HF-загрузки).
- **(a) Кросс-энкодер bge-reranker-v2-m3** (`reranker_model.py`, transformers на MPS) — *основной*
  прод-путь. **Измерено:** top-1 84%→**92%**, вещество+раздел 64%→**68%**, плант-скоуп 100%,
  latency **818 мс** (вкл. вызов Ollama-эмбеддера; сам реранк 20 кандидатов на MPS — десятки мс).
  Загрузка с HF флапала (зависала на 1.5/2.3 ГБ), вылечено робастным резюмом `snapshot_download`.

**Итоговая таблица реранка (28 запросов, полная база):**
| | без реранка | LLM (qwen2.5) | кросс-энкодер |
|---|---|---|---|
| top-1 | 84% | 88% | **92%** |
| top-3 | 96% | 92% | 92% |
| вещ+раздел | 64% | 68% | **68%** |
| плант | 100% | 100% | 100% |
| latency | ~210 мс | 6249 мс | **818 мс** |

Вывод: кросс-энкодер бьёт LLM по обоим осям (точнее + ~8× быстрее) → дефолт. LLM-реранк —
запасной air-gapped-вариант. **Цепочка масштаба: лексика 60% → семантика 84% → +реранк 92%.**
Проводка: `service.py` `RERANK=1`, `RERANK_BACKEND=crossencoder|llm`, отказоустойчиво
(реранкер не загрузился → сервис без него). `/health` отдаёт активный `rerank`.
Eval: `semantic_rerank_eval.py` (env `RERANK_BACKEND`).

### 6. Находка: 27.6% базы 2601 — артефакты HTML-ингеста ГН
Анализ имён `data/substances_all.json` (read-only):
- **207 имён с «+»** — в ГН 2.2.5 «+» = «опасно при попадании на кожу» (аннотация, не часть имени).
  Из них **только 3 — истинные дубли** (азотная/бензол/серная кислота+, есть парная запись без «+»),
  остальные 204 — реальные вещества с приклеенным маркером. На «первая помощь при попадании
  азотной кислоты НА КОЖУ» победила «азотная кислота+» — формально «промах» в eval, по сути ВЕРНЕЕ.
  → часть промахов top-1 = артефакты данных/теста, ретривер лучше, чем показывает 92%.
- **54 строки-контроли** («б) контроль по формальдегиду») — не мусор, а регуляторные записи
  (ПДК контролируется по прокси-веществу); чистить осторожно, не удалять.
- **481 аномально длинное имя** (часть — легитимные смеси, часть — мусор).
- **ВАЖНО про verified-ядро:** отличается по `confidence` (high/medium/low = 48 шт.), НЕ по
  `source_tier` (T1 назначен широко — 2590 записей, верифицированность не маркирует). Все артефакты
  («+», контроли) — только в `needs_review` (2553), **ни одного внутри verified-48 (проверено)**.
  [Исправление прежней записи: ранее ошибочно написал «verified-48 = T1» — верно «confidence ≠ needs_review».]
- **Задача чистки (под аудит-спринт):** «+» → поле `skin_hazard`, слить 3 дубля, пометить
  строки-контроли. Это и есть «грабля грязных данных», за которую платят.

### 7. Чистка данных (недеструктивно) + Qdrant
**Чистка** (`clean_data.py`, originals не тронуты → `corpus_full_clean.json`, `substances_clean.json`):
детерминированный transform чанков — «+»→поле `skin_hazard` (2040 чанков), строки-контроли→
`is_control` (540), слиты 3 истинных дубля. Итог: 26010→**25980** чанков, 2601→**2598** веществ.
Реиндекс клина — отдельные файлы (`embeddings_clean.npy`), билд параметризован env
`CORPUS_FILE/EMB_FILE/IDS_FILE`. Шёл ~14 мин (31 эмб/с, модель горячая).

**Qdrant** (`qdrant_index.py`) — прод-векторная БД, зрелость сервиса. Docker-демон лежал →
использован **локальный on-disk режим** (тот же код, что серверный, меняется URL). Билдер +
`QdrantRetriever` (тот же `.query`-интерфейс). **Плант-скоуп = настоящий payload-фильтр**
(`substance IN scope`), а не доп. к скору — чище numpy. **Паритет с numpy-семантикой: 6/6**
(вкл. плант: Корунд→цианид калия, ПЛОЩАДКА→метилакрилат). Подключён в сервис: `RETRIEVER=qdrant`.
Нюанс: локальный режим qdrant предупреждает про >20k точек — для прода Qdrant в Docker.

### 8. Расширенный eval (47 вопросов)
`eval_extended.py` — 25 исходных + **22 новых** по verified-ядру (заземлены на реальные ПДК/класс/
вспышку, ответ однозначен). Семантика без реранка, грязная база:
| | 25 вопросов | 47 вопросов |
|---|---|---|
| top-1 | 84% | **89%** |
| top-3 | 96% | **98%** |
| вещ+раздел | 64% | 62% |
**Метрика держится на почти удвоенном наборе.** Из 5 промахов **лишь 1 — реальный семантический
провал** («ожог глаз щёлочью → ораза»); остальные 4 — артефакты/дубли ингеста (контроль-строка
формальдегида, «азотная кислота+» = кожная запись→по сути вернее, «борная кислота (ортоборная)» =
синоним-дубль, «канцероген группа 1» = неоднозначный тест). Подтверждает находку №6.
Ретривер `CORPUS_FILE/EMB_FILE`-параметризован → тот же eval на чистой базе.
**Прогрессия чистки top-1: 89% (грязная) → 91% (3 дубля) → 94% (7 дублей, 25940 чанков)**
(top-3 98%, вещ+раздел 66%, latency 173 мс). Чистка убрала промахи «азотная кислота+» и
«борная кислота (ортоборная)». Остаток: контроль-строка
формальдегида, «борная кислота (ортоборная)» = синоним-дубль в скобках (не «+», мой проход не
трогал), «ораза/щёлочь» (реальный gap), «канцероген группа 1» (неоднозначный тест).
→ скобочные дубли слиты в п.11 (4 шт.: борная кислота, бутилацетат, тетрафторметан, этилацетат).

### 10. OCR-пайплайн для грязных PDF (Ступень-1, аудит-спринт)
`ingest_pdf.py` — доказательство «грабли грязных данных». Локальный `GN_..._compressed.pdf` —
**чистый скан: 469 страниц, текстовый слой = 0** (проверено pdfplumber + PyMuPDF).
Пайплайн: детект слоя → нет → растеризация (**PyMuPDF**, без системного poppler) → OCR
(**easyocr**, ru+en, torch/**MPS**, on-prem). Капстоун-прогон 2 страниц:
- стр 0: 1661 симв за 77 с (прогрев модели), стр 1: 2094 симв за 2.5 с (горячая) — на MPS.
- извлёк живой текст: «ГЛАВНЫЙ ГОСУДАРСТВЕННЫЙ САНИТАРНЫЙ ВРАЧ … СанПиН 1.2.3685-21».
- **с реалистичным шумом** («0б»→«Об», «0LdQ1», обрезки) — ровно та грязь, ради которой
  нужна ручная верификация. Это честная демонстрация, а не «всё идеально».
Вывод: прод-ингест реальных паспортов завода = OCR-стадия + верификация + парсинг таблиц.
Капстоун интеграции: полный прод-стек `RETRIEVER=semantic + RERANK=crossencoder + LLM=ollama`
проверен в `/ask` end-to-end (семантика→реранк→qwen2.5, ответ с цитатами).

### 11. АХОВ-слой + слияние скобочных дублей
**Скобочные дубли:** `clean_data.py` обобщён — `dupe_target()` ловит дубль по «+» ИЛИ хвостовой
«(…)» (только если база без скобок есть отдельно; из 1428 «(…)» дублей ровно 4). Итог чистки:
26010→**25940** чанков (7 дублей: 3 «+» + 4 скобочных), 2601→**2594** вещества. Ре-реиндекс (56 эмб/с).

**АХОВ-карточки** (`data/ahov_cards.json`, 22 вещества опасного ядра) — аварийный слой:
ООН-номер, класс ДОПОГ/ADR, признак АХОВ (токсичное облако), аварийная заметка. Эндпоинт
`/emergency/{name}` отдаёт карточку + первую помощь/СИЗ/ПДК из паспорта. **Safety-честность:**
только канонические ООН-номера (не выдумано), МДИ опущен (неуверен), зоны эвакуации НЕ приведены
(зависят от количества — нельзя без расчёта), весь слой = `needs_review` «сверить по офиц. таблице».
Проверено: цианид натрия→UN 1689/6.1, фосген→1076/2.3, толуол→не АХОВ/1294/3.

### 12. UI `/emergency` + находка «реранк vs чистка»
**UI** (`emergency.html`, отдельная лёгкая страница, не трогает 411КБ `index_pro.html`).
Переработан по `ui-ux-pro-max` (дизайн-система: Dark Mode OLED, safety-blue+orange, шрифты
data/precise): ops-дашборд — SVG-иконки (не emoji), моно табличные цифры для ООН/ДОПОГ/ПДК,
цветовая severity-полоса (красная АХОВ / зелёная не-АХОВ), статус-индикатор сервиса (пинг /health),
skeleton при загрузке, focus-кольца, `prefers-reduced-motion`, адаптив 560px, контраст AA.
Шрифты — системный моно/sans-стек (on-prem: без Google CDN). Проверено скриншотами (playwright):
фосген (АХОВ, ООН 1076) и толуол (не-АХОВ, ООН 1294) рендерятся корректно.
**Баг найден и исправлен:** UI из `file://` → `fetch` к сервису = cross-origin; у FastAPI не было
CORS → ответ блокировался. Добавлен `CORSMiddleware(allow_origins=["*"])` (внутренний on-prem).

**Находка (реранк на чистой+расширенной базе):**
| набор/конфиг | top-1 | top-3 |
|---|---|---|
| 28q грязная + реранк | 92% | 92% |
| 47q чистая, без реранка | **94%** | **98%** |
| 47q чистая, + реранк | 94% | 96% |
Реранк **НЕ улучшил** чистую базу и внёс новый промах (ортофосфорная→ортофосфористая кислота,
путаница близких синонимов). Причина: **реранк и чистка целились в одни и те же промахи**
(артефакты/near-дубли) — двойного счёта нет. Вывод: **семантика + чистка = 94%, реранк
опционален** (нужен на грязных данных, нейтрален/слегка вреден на чистых). Не смешивать в питче.

### 13. Живая операторская консоль `console.html`
Единый интерфейс к **боевому** семантическому сервису (в отличие от замороженного офлайн-индекса
на 48 веществ в `index_pro.html`): два режима в одном поле ввода.
- **«Поиск»** → `GET /search?q=&k=5`: карточки результатов с веществом, разделом, score-баром,
  цитатой (ГОСТ-формат «вещество (формула, CAS), Раздел N») и сниппетом текста.
- **«Спросить»** → `POST /ask` с опциональным плант-скоупом: grounded-ответ qwen2.5 + чипы-источники.
- Дизайн-токены общие с `emergency.html` (`ui-ux-pro-max`: Dark OLED, safety-blue/orange,
  системный моно/sans-стек, SVG-иконки), статус-индикатор `/health`, skeleton, focus-кольца,
  `prefers-reduced-motion`, поле адреса сервиса (по умолчанию `:8000`).
- **Проверено скриншотами** (playwright, сервис `RETRIEVER=semantic LLM_BACKEND=ollama`):
  «первая помощь при отравлении хлором» → top-1 **хлор** 0.769, 5 ранжированных карточек;
  «как безопасно хранить серную кислоту» → grounded-ответ со ссылкой `[1]` на серную кислоту /
  Раздел 7 и тремя чипами-источниками. Оба режима рендерятся корректно.

### 14. Лучшие практики химпрома: совместимость, GHS, зона АХОВ, гигиена (4 фичи)
Сверились с индустрией (SDS-софт, нормы сегрегации, СГС/GHS, методики МЧС) и добавили то,
что строится на НАШИХ данных или на канонических стандартах — без фабрикации регуляторики.

**14.1 Матрица несовместимости хранения** (`engine/compat.py`, `/compat?a=&b=`).
Обоснование: по U.S. Chemical Safety Board несовместимое хранение — фактор в ~25% из 1200
химинцидентов 2000-2020. Логика: вещество относим к сегрегационным группам (кислоты/щёлочи/
окислители/горючие/активные металлы/токсичные газы/цианиды/выделяющие хлор/орг.растворители)
**по идентичности** (имя, класс, собственные GHS-метки), затем канонические правила группа×группа
(NFPA/CSB) дают вердикт forbidden/caution/ok + цитата Раздела 10 обоих паспортов.
- **Баг и фикс (важный методический):** сначала классифицировал по тексту несовместимости
  («вдали от окислителей» ловилось как «окислитель») — вещество получало чужую группу.
  Текст несовместимости описывает *с чем нельзя*, а не *чем вещество является*. Переписал
  строго по идентичности. До фикса: аммиак+хлор давал «OK» (хлор не распознан как газ) — опасно;
  после: распознаётся; гипохлорит+кислота даёт правильную причину (выделение Cl2), а не «H2 от металла».
- Проверено: H2SO4+NaOH→forbidden, H2SO4+цианид→forbidden(HCN), гипохлорит+HCl→forbidden(Cl2),
  HNO3+толуол→forbidden(окислитель+горючее), толуол+метанол→ok.

**14.2 GHS-слой** (`engine/ghs_map.py`, `/ghs/{name}`). 9 стандартных пиктограмм СГС (ООН) +
сигнальное слово Опасно/Осторожно — выведены из GHS-меток паспорта + класса ГОСТ 12.1.007.
H-коды сверх имеющихся не присваиваем. Уточнил сигнальное слово по СГС (острая токсичность кат.1-3
и H370 → «Опасно»). Пометка confidence: from_labels / derived (если меток нет — по классу опасности).

**14.3 Расчёт зоны заражения АХОВ по РД 52.04.253-90** (`engine/rd_zone.py`, `data/rd_ahov.json`,
`POST /zone`). Закрывает сознательный пробел («зоны не давали без расчёта»): теперь даём по
утверждённой методике ГО. **Коэффициенты верифицированы web-research-агентом по 3 независимым копиям
документа** (stroyinf.ru, МЧС Красноярского края, libr.aues.kz) — выловлены 2 опечатки КонсультантПлюс
(HBr K3, HCl K7). Считаем Qэ1 (первичное облако), Qэ2 (вторичное), глубину по таблице Прил.2.
- 11 АХОВ с табличными коэффициентами; **диборан и BF3 ЧЕСТНО отклоняются** (нет в перечне 34 АХОВ РД).
- Глубина по колонке ветра 1 м/с (макс./консервативно); предельный перенос не применяется (в безопасную сторону).
- Сверка с каноническими примерами: хлор 40т/изотермия/1м/с → Г≈15 км; HCN K1=0 → только вторичное облако. needs_review.

**14.4 SDS-гигиена** (`engine/hygiene.py`, `/passport/{name}`, `/quality`, `/audit`).
Из обзора SDS-софта (аудит доступа, полнота паспортов) — БЕЗ выдумывания дат пересмотра.
Аудит-лог запросов (append-only jsonl, на /search /ask /compat /zone), состояние паспорта
(полнота критичных полей + верификация), дэшборд качества.
- **Честная находка дэшборда:** полны только **44 паспорта (1.7%)**; 2546 — needs_review и без
  СИЗ/первой помощи/хранения (богатые данные только у курированного ядра). Это и есть ценность
  инструмента — он показывает, какие паспорта дозаполнять перед оперативным применением.
- Побочно: чистка не была подключена к сервису — retriever грузил `substances_all.json` (грязный,
  хлор как «хлор+»). Сделал файл веществ env-настраиваемым (`SUBS_FILE`), сервис теперь на чистом.

**UI** (`safety.html`): единая страница, 4 вкладки (Совместимость/GHS/Зона АХОВ/Качество) в общей
дизайн-системе (`ui-ux-pro-max`: Dark OLED, моно табличные цифры, focus-кольца, `prefers-reduced-motion`).
GHS-пиктограммы как красные ромбы СГС (белый ромб, красная рамка, чёрный символ). **Проверено
скриншотами playwright** против живого сервиса — все 4 вкладки рендерятся, аудит-лог пишет запросы.
Доп. эндпоинт `/substances/names` для автодополнения. Исправлен забытый listener смены адреса сервиса.

### 15. Обогащение приоритетных АХОВ + сквозное демо
**Находка от дэшборда гигиены:** дыры узкие, не массовые. Из 27 приоритетных АХОВ (rd_ahov ∪ ahov_cards)
почти все полны; реально не хватало данных только у **хлора** (флагман — пустые СИЗ/первая помощь/
хранение/GHS) и отсутствовали 4 вещества (сероводород, диоксид серы, хлороводород-газ; ТДИ оказался
уже полным — ранее ложно числился отсутствующим из-за нормализации имени).

**Обогащение (`data/ahov_enrich.json` + `engine/apply_enrich.py`).** Web-research-агент собрал
**верифицированные** оперативные поля (ПДК по ГН 2.2.5.3532-18, класс, СИЗ, первая помощь, хранение,
GHS) по 5 АХОВ — с источниками (паспорта s-gas.com, CAMEO/NOAA, материалы МЧС/ГО) и **честными
критическими оговорками**: расхождение источников по кислороду при хлоре и ИВЛ при SO2, спор о классе
HCl (ГН даёт 2), single-source ПДК ТДИ. Спорные поля помечены `needs_review`.
- Merge **недеструктивный**: режим `fill_empty` (только пустые поля, существующие данные не трогаем) /
  `add` (новые вещества); бэкап `substances_clean.bak.json`; идемпотентно; пометка `enriched:true`.
- Результат: хлор GHS пустой → **GHS03/04/06/07/09 + «Опасно»** (from_labels, не derived); +3 вещества
  (2594→2597); complete 44→47. Хлор остаётся `needs_review` ЧЕСТНО (первая помощь спорна в источниках).
- Дэшборд complete% почти не вырос (1.7→1.8%) — bulk 2546 по-прежнему без оперативных полей; обогащали
  только критичные АХОВ. Это честно отражено, не маскируется.
- Нюанс: 4 новых вещества пока вне семантического корпуса (нет чанков/эмбеддингов) — доступны через
  структурные слои (GHS/compat/emergency/passport/zone), но не через /search. Расширение корпуса — отдельная задача.

**Сквозное демо.**
- `home.html` — хаб-страница (дизайн-система): живые метрики (/health), плитки на console/emergency/safety/
  офлайн-демо, ссылки на DEMO.md/WORKLOG/README. **Баг и фикс:** вложенный `<a>` внутри `<a class=tile>`
  (невалидный HTML) рвал DOM — текст плитки вываливался наружу; убрал вложенную ссылку. Проверено скриншотом.
  Не трогал существующий `index.html` (отдельная офлайн-демо) — хаб сделан новым файлом.
- `DEMO.md` — питч-сценарий на ~7 мин вокруг хлора: оператор спрашивает (console) → совместимость →
  GHS → расчёт зоны (РД, ~15 км) → аварийная карточка → дэшборд честности. С аргументами и эндпоинтами.

### 16. Расширение семантического корпуса на обогащённые/новые АХОВ
После обогащения (п.15) новые вещества были доступны через структурные слои (GHS/compat/emergency),
но НЕ через `/search` — в корпусе не было их разделов. А у хлора чанки остались со старыми пустыми
СИЗ/первой помощью (обогащение было после генерации корпуса). Закрыл инкрементально (`engine/extend_corpus.py`).
- **REFRESH хлора:** перегенерировал 10 разделов из обогащённой карточки и **пере-эмбеддил строки НА ТЕХ ЖЕ
  позициях** — выравнивание `chunks[i] ↔ emb[i]` сохранено (retriever мапит позиционно, проверено).
  Заодно ушёл артефактный `doc_id "хлор+"` → чистый «хлор».
- **ADD 3 веществ** (сероводород, диоксид серы, хлороводород): по 10 разделов, эмбеддинг bge-m3, дописаны
  в конец корпуса и матрицы. 25940 → **25970 чанков**. Бэкапы corpus/emb. Идемпотентно. ТДИ не трогал
  (его чанки уже были, обогащение добавило только skin_hazard — в текст разделов не входит).
- **Проверка /search (семантика):** сероводород→top-1 0.75 (Разд.4), диоксид серы→0.68, хлороводород→0.61,
  хлор→0.75 с НОВЫМ текстом первой помощи («НА НОСИЛКАХ, 5% сода»). Grounded `/ask` по сероводороду
  опирается на новые чанки (кислород ПОКАЗАН, антидота нет) с корректными цитатами.
- Ключевой момент инжиниринга: индекс расширяется БЕЗ полного пересчёта (~27 мин) — только 40 новых/
  изменённых векторов; позиционная синхронизация corpus↔embeddings строго выдержана (assert на длины).

### 17. Bulk-покрытие 2545 веществ: слой типового руководства по группе (честно, без фейк-заполнения)
Задача: у ~2545 веществ bulk-базы (ингест ГН) есть идентификация+ПДК+класс, но нет паспортных
СИЗ/первой помощи/хранения. **Сознательно НЕ заполнял поля выдуманными данными** — это та самая
ложная полнота, которую дэшборд честно вскрывал, и в безопасность-критичном домене она опасна.

**Решение (`engine/baseline.py`):** ВЫЧИСЛЯЕМЫЙ слой типового руководства по химической группе.
- Классифицируем вещество (переиспользуем `compat.classify`: кислоты/щёлочи/окислители/токсичные газы/
  цианиды/активные металлы/орг.растворители) + класс опасности → консервативные групповые шаблоны
  СИЗ/первой помощи/хранения (общая практика ГОСТ 30333, НЕ паспорт вещества).
- **Данные вещества не мутируются** (пусто = пусто, честно). Руководство отдаётся отдельным блоком
  с пометкой источника: `паспорт` vs `типовое по группе`. Паспортные поля всегда приоритетны.
- `data_grade`: passport (есть СИЗ+ПП+хранение) / partial / baseline (типовое). Распределение: **52 / 0 / 2545**.
- Усиление защиты дыхания для класса 1-2, если СИЗ типовые. Дальнейший фикс `compat`: ужесточил
  исключения для «кислот» (желчные/жирные/соль/натрий/ангидрид) — лекарство «аллохол» больше не кислота;
  ключевые кислоты (серная/азотная/соляная/акриловая) сохранились.

**Интеграция.** Эндпоинты: `/guidance/{name}`, `/substance` (блок guidance), `/emergency`
(СИЗ/ПП фолбэк на типовое с явной пометкой источника), `/quality` (распределение data_grade).
- **UI safety.html** (вкладка «Качество»): карточки 52 паспортных / 2545 типовых / **100% с оперативным
  руководством**; бары data_grade; блок «дыры в паспортных полях» ОСТАЁТСЯ — реальный пробавер не маскируется.
- **UI emergency.html**: у bulk-вещества первая помощь/СИЗ теперь не «—», а групповое руководство
  с жёлтым тегом **«типовое по группе»**. Проверено скриншотами.

**Что НЕ делал и почему:** не перегенерировал корпус под bulk (2545×разделы = полный пересчёт ~17 мин);
корпус уже рендерит generic-фолбэк в Разделах 6/8, а богатое групповое руководство несёт структурный
слой/UI. Массовая ВЕРИФИЦИРОВАННАЯ паспортизация bulk — отдельная задача (дэшборд показывает её масштаб
честно: 2545 в графе «дозаполнить верифицированно»).

### 18. Дежаргонизация UI + инструкция + анализ рынка
**Дежаргонизация (по обратной связи «человеку непонятно»):** убрал из всех 4 UI техжаргон и
англицизмы, заменил на понятный русский. Имена моделей (bge-m3/qwen2.5/Ollama) → «поиск по смыслу»,
«top-1 94%» → «точность поиска нужного вещества», «on-prem» → «локально, на оборудовании предприятия»,
«GHS» → «знаки опасности (СГС)», «data_grade» → «полнота сведений», «needs_review» → «требует проверки»,
«сервис/retriever» → «адрес системы / система на связи», endpoint-теги → русские подписи,
«Аудит-лог» → «Журнал запросов». Почистил и бэкенд-дисклеймеры (compat/ghs/baseline): «NFPA/CSB»,
«SDS», «CLP» → понятные формулировки. Оставлены стандартные русские сокращения (АХОВ/ПДК/СИЗ/ДОПОГ/
ГОСТ/РД/СГС/ООН). Добавлены подсказки «как пользоваться» на каждой вкладке. Заголовки страниц → русские.

**Инструкция (`guide.html`):** отдельная страница в дизайн-системе — 7 пронумерованных разделов с
оглавлением-чипами, для каждого инструмента пример «ввод → результат» (аммиак/хлор/фосген/
гипохлорит+соляная кислота), цветовые пометки, подсказки, режим печати (повесить у рабочего места).
Ссылка на главной. Проверено скриншотом.

**Анализ рынка (`MARKET.md`):** web-research конкурентов. Ключевое: международный SDS/EHS-рынок ($2млрд)
весь в SaaS (запрет для КИИ); реальный AI только у VelocityEHS (Ask Vēlo) и Benchmark Gensuite (Genny AI) —
облачные, англоязычные, по OSHA/GHS, не знают ГОСТ. РФ-рынок: 1С (ОТ/СОУТ без AI), ТОКСИ+/ПК «Русь»/
«АХОВ» (только зоны, без связи с паспортами), Техэксперт (архив НПА). **Ни один РФ-продукт не объединяет
паспорта+зоны+совместимость+аварийные карточки; русского AI-помощника по химбезопасности как продукта
нет.** Наша отстройка: on-prem + русская семантика + интеграция 4 функций + честность данных.
Приоритеты улучшений (перенять у рынка): QR на рабочих местах, реестр химикатов по площадкам,
версионирование паспортов, мобильный офлайн; далее — экспорт этикеток СГС, интеграция с 1С, обучение/тесты.

### 19. Реализация рыночных улучшений: реестр, этикетки СГС+QR, мобильная карточка, квиз
Сделал параллельно всё реализуемое из приоритетов §18 — **без выдумывания данных** (всё считается из
имеющихся паспортов/групп либо помечается «типовое по группе»). Версионирование и авто-обновление
паспортов **сознательно отложены** — нет реального потока ревизий, фейк-история недопустима.

**Реестр по площадкам (`registry.py` + `registry.html`):** для каждого из 13 заводов (связка
`plants_linked`) показываем его вещества (класс/ПДК/группы/полнота данных) и **автоматически находим
опасные пары для совместного хранения** попарным прогоном через `compat.check`. Пример: ПЛОЩАДКА (7 веществ)
→ 7 пар «нельзя» (акриловая кислота + аммиак = кислота+щёлочь, бурная нейтрализация) + 4 «осторожно».
Количество/место/ответственного **не синтезируем** — это поля под заполнение предприятием. Сортировка
заводов по числу опасных пар. UI: выбор завода → сводка + список конфликтов (красный «нельзя» / жёлтый
«осторожно» с цитатой причины) + таблица веществ со ссылками карточка/этикетка/квиз.

**Экспорт CSV (`/export/registry.csv`):** реестр в CSV для Excel/1С — BOM + `;`-разделитель (кириллица в
Excel), пустые колонки Количество/Место/Ответственный под ручное заполнение. *Баг и фикс:* кириллическое
имя файла в `Content-Disposition` падало на `latin-1`-кодировке HTTP-заголовка (`UnicodeEncodeError`) →
ASCII-имя по ИНН + `filename*=UTF-8''…` (RFC 5987) для кириллицы в браузере/Excel.

**Этикетка СГС + QR (`label.html` + `/qr` + `/card`):** печатная этикетка для тары/рабочего места —
название, формула, CAS, сигнальное слово, **знаки опасности СГС** (красные ромбы, те же 9 пиктограмм
из safety.html), класс, ПДК, хранение, **QR-код**. QR (`/qr/{name}`, `segno` 1.6.6, чистый python, SVG)
ведёт на `/card/{name}` — мобильную карточку вещества (АХОВ-баннер ООН/ДОПОГ, сигнальное слово, класс,
ПДК, первая помощь, СИЗ, хранение, пометки «типовое»). Навёл телефон у ёмкости → видишь первую помощь.
Режим печати (CSS `@media print`) скрывает служебное, печатает чистую этикетку/PDF.

**Квиз-инструктаж (`quiz.py` + `quiz.html`):** короткая проверка знаний по веществу для обучения персонала.
**ОСОЗНАННО детерминированный, без LLM** — в безопасность-обучении галлюцинации недопустимы: вопросы и
ответы берутся прямо из паспорта. Выбор ответа для класса опасности и ПДК (правдоподобные отвлекающие —
кратные ПДК), открытые вопросы по СИЗ/первой помощи/хранению с эталоном и пометкой источника
(паспорт/типовое). UI: автоподсчёт баллов, подсветка верного/неверного, раскрытие эталонов.

**QR в аварийную карточку (`emergency.html`):** добавил блок «Карточка на телефон» — QR на `/card`,
чтобы реагирующий открыл ту же карточку на смартфоне у места аварии.

**Бэкенд:** +6 эндпоинтов (`/registry`, `/registry/{name}`, `/export/registry.csv`, `/quiz/{name}`,
`/qr/{name}`, `/card/{name}`), `segno` через try/except (если нет — `/qr` отдаёт 503, не валит сервис).
Все проверены curl-смоуком; 5 UI-страниц (реестр/этикетка/квиз/аварийка+QR/главная с 3 новыми плитками)
проверены скриншотами playwright — консоль чистая, пиктограммы и QR рендерятся. Главная: 7 плиток.

### 20. Обогащение из мировых/российских открытых данных (4 источника + бонус)
Ресёрч (3 параллельных агента) по открытым данным мира и РФ → интегрировали то, что **реально открыто,
авторитетно и применимо к русским паспортам**, БЕЗ выдумывания значений (каждое число — из источника
с провенансом и `needs_review`).

**(1) IDLH — острый порог (NIOSH, public domain).** У нас был только хронический ПДК рабочей зоны;
добавили острый порог «немедленно опасно для жизни» — концентрацию, выше которой без изолирующего СИЗ
гибель за ~30 мин. `engine/fetch_idlh.py` тянет НАПРЯМУЮ с cdc.gov/niosh/idlh по CAS (парсит «Revised IDLH»,
NIOSH REL, основание), `data/idlh.json`. 28 ключевых АХОВ/веществ: хлор 10 ppm, аммиак 300, фосген 2,
сероводород 100, ТДИ 2.5 — эталонные значения первоисточника. NIOSH покрывает ~500 веществ; для остальных
честно ничего не пишем. Показываем в /substance, /emergency, /card, карточке вещества.

**(2) Русские синонимы (Wikidata, CC0).** Чинит recall поиска: «оксиран / окись этилена / этиленоксид»
→ одно вещество (PubChem прошлый раз не зашёл — англоцентричен). `engine/fetch_synonyms.py` через ОСНОВНОЙ
API Wikidata (haswbstatement P231=CAS → QID, затем wbgetentities батчами) — НЕ WDQS/SPARQL (тот резал до
1 req/min). *Баг и фикс:* при первом прогоне поиск QID ловил 429 и кэшировал `None` навсегда (хлор/бензол
есть в Wikidata, но закэшены None) → добавил бэкофф в `_api`, очистил None из кэша, перезапустил. QID кэшируются
(`data/_wd_qid_cache.json`) — возобновляемо. Покрытие: Wikidata знает ~274 из 2029 наших CAS, с рус. метками
меньше — синонимы получают распространённые вещества (которые реально ищут), не все 2597. Резолвинг по
синониму встроен в /substance, /emergency, /compat, /card, /ghs, /quiz + автодополнение.

**(3) СанПиН 1.2.3685-21 — расшифровка особых отметок.** Сами ПДК уже из ингеста ГН/СанПиН; новое —
**расшифровка кодов О/К/А/Ф/кожа**, которые УЖЕ в базе (423 шт.), но в UI не раскрыты, а они безопасно-критичны
(«О» = нужен непрерывный контроль воздуха, «К» = канцероген — меняет весь режим). `engine/sanpin.py`.
*КРИТИЧЕСКИЙ баг и фикс:* поле `special` у вручную-обогащённых АХОВ перезаписано свободным текстом
(«…ОКИСЛИТЕЛЬ…»), и посимвольный декодер ловил буквы → ложно клеил хлору «канцероген». Сделал СТРОГИЙ
парсер: декодируем только если ВСЯ строка из символов-кодов; текст игнорируем. Проверка по всей базе:
367 веществ с кодами, **0 ложных срабатываний**. Показываем в /substance, /emergency, /card, /sanpin.

**(4) Официальные пиктограммы СГС (public domain, Wikimedia).** Заменяем рисованные ромбы официальными
UN-пиктограммами (`assets/ghs/*.svg`) с graceful-fallback на рисованные при отсутствии файла (onerror).
Качались с бэкоффом на 429 (конкуренция за IP с задачей синонимов).

**Бонус — ERG-2024 + ООН/ДОПОГ (US/Canada gov, public domain).** Ресёрч-агент верифицировал по CAMEO/ADR
(3 источника/вещество) расстояния изоляции и эвакуации + классы ДОПОГ для 7 АХОВ. `data/erg_ahov.json`,
эндпоинт `/erg`. *Закрыл найденный пробел:* хлор не был помечен АХОВ (нет карточки в ahov_cards) — теперь
АХОВ через ERG: ООН 1017, ДОПОГ 2.3+5.1+8, зона изоляции 61 м / эвакуация день 0.32 ночь 1.45 км (малый),
610 м (большой, с оговоркой про Таблицу 3 ERG). Явно помечено: ориентир первого реагирования (методика
США/Канады), НЕ заменяет расчёт по РД 52.04.253-90.

**Единая карточка вещества (`substance.html`).** Связала всё: один экран — идентификация+синонимы, класс/ПДК/
IDLH/сигнальное слово, знаки СГС, отметки СанПиН, АХОВ ООН/ДОПОГ+эвакуация ERG, первая помощь/СИЗ/хранение
(с пометками источника), кнопки на этикетку/квиз/QR-карточку/аварийку. Стала центральной плиткой на главной
(теперь 8 плиток). Проверено скриншотами playwright (хлор — все слои на месте, fallback пиктограмм работает).

**Поймано 3 реальных бага** (None-poisoning кэша QID; ложный «канцероген» из текста; «нет АХОВ-карточки» при
наличии ERG) — все исправлены. +5 эндпоинтов (`/sanpin`, `/erg`, + IDLH/синонимы/флаги в существующих).

### 21. Широкий проход по открытым данным (масштабирование + слой эталонной классификации)
По запросу «бери все открытые данные, до которых дотянешься» прогнали сборщики по **всей базе** (2072 CAS),
не только по ключевым АХОВ. Принцип тот же: ничего не выдумываем, всё с провенансом и `needs_review`.

**IDLH — со всей базы (NIOSH).** `fetch_idlh.py` опросил все 2072 CAS напрямую с cdc.gov/niosh/idlh →
**207 веществ с острым порогом** (было 28). NIOSH покрывает ~500 веществ всего, так что 207 — это близко
к потолку пересечения с нашей номенклатурой; для остальных честно ничего не пишем («нет в NIOSH»).
Примеры подтянутого: метилакрилат 250 ppm, N,N-диметилацетамид 300 ppm. Все значения — числа первоисточника
(ppm/мг·м⁻³), `needs_review`. Уже в /substance, /emergency, /card, карточке вещества.

**Слой эталонной GHS-классификации PubChem (NIH, public domain) + СВЕРКА.** Наши знаки СГС выведены из
меток паспорта эвристически; PubChem (агрегат ECHA/NITE/др.) даёт ЭТАЛОННЫЙ набор пиктограмм + H-кодов
с провенансом. `engine/fetch_pubchem.py` (CID по CAS через PUG-REST → GHS Classification через PUG-View),
`engine/pubchem.py` (потребитель: расшифровка H-кодов **официальными формулировками ГОСТ 31340-2013**,
~50 кодов, + функция сверки). `data/pubchem_ghs.json` — **1493 вещества с GHS** (опрошены все 2072 CAS; заливка инкрементальная
и резумируемая). Эндпоинты `/pubchem/{name}` и сверка внутри `/ghs/{name}`.
*Зачем сверка — это аудит-сигнал:* для хлора наши 5 знаков совпали с PubChem, но PubChem добавляет **GHS05
(коррозия) и GHS08 (хроническое здоровье)**, которых у нас не было — то есть наша эвристика недоклассифицировала.
`crosscheck` показывает `match / only_ours / only_pubchem` — расхождение = флаг аудитору, а не молчаливая подмена.
H-коды отдаются с русским текстом ГОСТ 31340 (H330 «Смертельно при вдыхании», H314 «…вызывает химические ожоги»).

**Синонимы — добиты до потолка Wikidata.** После фикса None-poisoning и таргетного перезапуска: **146 веществ /
379 синонимов** (флагманы сверены: хлор→дихлор/молекулярный хлор, аммиак→гидрид азота, бензол→фен/бензен).
Это потолок для нашей номенклатуры — Wikidata знает ~274 наших CAS, с русскими метками ещё меньше.

**Карточка вещества (`substance.html`) — финальная проверка скриншотом (хлор):** тайлы класс 2 / ПДК 1 мг/м³ /
IDLH 10 ppm / «Опасно», 7 официальных пиктограмм СГС, список H-кодов с русским текстом, блок IDLH, отметка
СанПиН, АХОВ ООН 1017+эвакуация ERG, первая помощь/СИЗ — все слои на месте, fallback пиктограмм работает.

Итог прохода: IDLH 28→**207**, PubChem GHS-слой **1493** (опрошены все 2072 CAS), синонимы **146/379**.
Новых выдуманных значений — ноль; каждый показатель из авторитетного источника, сверка расхождений вместо тихой подмены.

### 22. Готовность к внедрению: доверие (отказ + верификация), прод-деплой, документы
Задача — закрыть всё, что технически возможно, до промышленного внедрения на завод (КИИ/152-ФЗ).
Честная граница: фактическую подпись эксперта под ИХ веществами, классификацию по КИИ, аттестацию ФСТЭК
и интеграцию с ИХ AD/LDAP мы делать не вправе — строим механизм, который это принимает.

**(1) Отказ вместо галлюцинации (safety-критично).** Главная дыра: `/ask` всегда генерировал по top-3,
даже если retrieval-score низкий (нет оснований в паспортах). Добавил гейт: если лучший score < `ABSTAIN_THRESHOLD`
— честный отказ без вызова LLM, со ссылкой на паспорт. Калибровка по корпусу: реальные запросы 0.66–0.75,
шум 0.43–0.53. **Новый eval `eval_abstention.py`** меряет не «попадание», а безопасность: 22 answerable +
15 unanswerable (вне домена). Поймал реальный ложный ответ («посчитать НДС» → 0.618). Порог поднят до **0.62**:
покрытие 100%, корректный отказ 100%, **галлюцинации 0%**. Порог калибруется на eval конкретного завода.

**(2) Слой экспертной верификации (`engine/verification.py`).** Развёл ДВА разных понятия: алгоритмическая
`confidence` (уверена машина) ≠ экспертная подпись (подтвердил человек завода). Для безопасно-критичного прода
нужна именно подпись. Статусы verified/needs_review/rejected, по умолчанию ВСЁ needs_review — система сама
ничего «verified» не ставит (иначе это выдумка доверия). Подпись ставит человек через `POST /verify`
(фиксируем кто/должность/дата, пишем в аудит; guard: verified без ответственного — отказ). Эндпоинты
`/verify`, `/verification` (сводка готовности: 0/2597 подписано — честно), `/verification/{name}`. Значок в
карточке вещества: «✓ Подтверждено экспертом — ФИО, дата» / «⚠ Требует проверки» / «✕ Отклонено». Атомарная
запись файла (`os.replace`) + перечитка по mtime — несколько воркеров видят чужие подписи.

**(3) Дисклеймер «поддержка принятия решений».** Постоянная плашка в карточке вещества и усиление в аварийной
карточке: первоисточник — паспорт (ГОСТ 30333), решение принимает ответственный, межд. данные (IDLH/GHS/ERG) —
справочные, зоны — по РД 52.04.253-90. Снимает вопрос ответственности на каждом экране.

**(4) Прод-деплой (субагент).** Починен баг `Dockerfile` (`COPY ../data` вне build-контекста — не собирался;
контекст → корень репо). Поймана сопутствующая дыра: `backends.py` не читал `OLLAMA_HOST` → в контейнере
генерация не достучалась бы до Ollama на хосте (починено, обратно-совместимо). Создано: `docker-compose.yml`
(сервис + опц. Qdrant профилем, healthcheck), `.env.example` (демо vs прод-КИИ), `DEPLOY.md` с **офлайн-гарантией**:
прогреплено `engine/*.py` — рантайм обращается ТОЛЬКО к локальному Ollama/Qdrant; все `fetch_*` и облачные
бэкенды (OpenRouter/GigaChat) вне боевого пути. Чеклист проверки air-gap (заблокировать трафик → /ask работает).

**(5) Пресейл-документы.** `docs/ВНЕДРЕНИЕ-дорожная-карта.md` (что готово / что на стороне завода / фазы
пилот→производство) и `docs/ЧЕКЛИСТ-приёмки.md` (верификация ядра, метрики отказа, КИИ-контур, актуализация —
с полями подписи ответственного).

Аудит-лог (`data/audit_log.jsonl`) уже был — теперь пишет и события `verify` (кто что подписал). Итог:
система отвечает по подтверждённому, помечает неподтверждённое, **молчит где нет оснований (0% галлюцинаций)**,
разворачивается офлайн в КИИ-контуре. Осталось плант-сайд: подпись эксперта, КИИ-классификация, AD/LDAP, железо.

### 23. Утилитарный слой эксплуатации (чтобы крутилось в цеху каждый день)
После Tier-0/Tier-1 — практические «скучные, но обязательные» вещи для ежедневной работы на проде.

**(1) Одна точка входа.** Было: 11 HTML-файлов, открываемых как `file://` — рабочему негде взять один URL.
Стало: сервис сам отдаёт UI (`/` → home, `/{page}.html` по whitelist, монтирование `/assets`). База API в
страницах переключена на same-origin при отдаче по http (демо-режим `file://` сохранён). Проверено: served-UI
по `http://host:8000/substance.html` грузится с 0 неудачных запросов (UI+ассеты+API с одного origin).

**(2) Деградация при падении Ollama.** Было: `OllamaLLM.generate` без try/except → если LLM недоступен,
`/ask` отдаёт 500 в лицо пользователю. Стало: на сбое/таймауте — откат на extractive (grounded-выжимка ровно
из разделов паспорта, без галлюцинаций) с пометкой «модель временно недоступна». Проверено на мёртвом хосте.

**(3) Опциональная аутентификация (API-ключ).** `@app.middleware` + `API_KEY` (по умолчанию ВЫКЛ — демо и
закрытый КИИ-контур). Включён → все запросы кроме `/health` и статики требуют `X-API-Key`. Это стопгап;
основная аутентификация на проде — обратный прокси с AD/LDAP (DEPLOY.md). Проверено: 401 без ключа, 200 с ключом.

**(4) Ротация аудит-лога.** Было: `data/audit_log.jsonl` рос безгранично. Стало: при превышении размера
(`AUDIT_LOG_MAX_BYTES`, дефолт 50 МБ) — ротация поколений (`.1`..`.3`), без внешних зависимостей.

**(5) Скрипт развёртывания `setup.sh`.** Один прогон от нуля: проверка python/пакетов, наличия `data/`,
доступности Ollama и моделей (bge-m3, qwen2.5), самопроверка `service.py` → запуск. Режим `--check` (без старта),
ориентир по железу в шапке. Проверено: `--check` зелёный.

`.env.example` дополнен (API_KEY, AUDIT_LOG_*). Итог: сервис — единый деплой с UI, не падает без Ollama,
с контролем доступа, без бесконечных логов и с воспроизводимым стартом.

### 24. Онбординг: чтобы незнакомый человек сразу начал работать
Проблема: входные экраны встречали пользователя пустым полем — непонятно, что вводить. Покрыты ВСЕ ШЕСТЬ
рабочих экранов (карточка вещества, аварийка, поиск/ответ, этикетки, квиз, реестр; последние три — параллельно
субагентами по тому же паттерну). Тач-френдли, гайдлайны ui-ux-pro-max: цели ≥38px, без hover-only:

- **Чипы-примеры в один тап.** Карточка: хлор/аммиак/серная кислота/ацетон/толуол. Аварийка: хлор/аммиак/
  сероводород/фосген/цианид натрия. Поиск: готовые вопросы («первая помощь при отравлении хлором» и т.д.,
  включают режим «Спросить»). Клик → подставляет и сразу выполняет.
- **Пустое состояние «как пользоваться»** (1-2-3) на каждом экране, прячется после первой загрузки. Объясняет
  что вводить, что покажет, и что пометки значат.
- **Сворачиваемая легенда терминов** в карточке вещества (progressive disclosure): простым языком — класс
  опасности, ПДК, IDLH, значок верификации.
- **Честный отказ виден глазами:** в поиске/ответе отказ рендерится янтарным «Нет основания для ответа»
  (а не как обычный ответ) — подкрепляет ключевой тезис «молчит, где не знает».
- Аварийка: подключён переход по `?name=` (из карточки вещества), пояснение красной/зелёной плашки АХОВ.

Проверено скриншотами: пустые состояния + загруженная карточка с легендой + визуал отказа; 0 JS-ошибок.

**Редизайн результатов «Поиск и ответ»** (были слишком плоские — текст-дамп). Ответ теперь структурирован:
нумерованные шаги с бейджами, критичное «ВНИМАНИЕ» вынесено в выделенный amber-callout с иконкой (safety —
предупреждение не теряется в тексте), цитаты [N] → карточки-источники со ссылкой на карточку вещества. Поиск:
карточки кликабельны → переход на полную карточку вещества, релевантность показана в % (вместо «голого» 0.682),
аккуратный сниппет по границе слова, иконка раздела. Проверено: клик ведёт на substance.html, отказ корректен, 0 ошибок.

### 25. Доверие во всём потоке + дашборд готовности к внедрению
Раньше статус верификации был только в карточке вещества. Теперь он виден везде, где система что-то утверждает:
- **`/search`** отдаёт статус верификации по каждому результату; в UI — компактный значок ✓/⚠/✕.
- **`/ask`** перестроен: retrieval идёт ВСЕГДА (дешёвый), кэшируется только дорогая генерация — поэтому
  `top_score`, `top_substance` и `verification` консистентны и для свежего ответа, и для кэш-хита. В UI ответа —
  значок верификации цитируемого вещества в шапке.
- **Дашборд `readiness.html`** (новый экран + плитка на главной): % готовности, счётчики (подтверждено/требует
  проверки/отклонено), инструкция как подписать (с готовой curl-командой под текущий адрес), таблица подтверждённых
  «вещество — кто — роль — когда» со ссылками на карточки. `/verification` дополнен списком `records`,
  в verification.py — `records(status)`. Это инструмент эксперта завода вести верификацию и видеть прогресс.
  Сейчас честно 0% (ни одной подписи до внедрения). Проверено скриншотами (пусто + с подписями), 0 JS-ошибок.

### 26. Полировка, качество ответов, рабочие сценарии
Догнал три оставшихся направления (после доверия-везде):
- **Полировка/единообразие:** emoji-иконки → аккуратные SVG (кнопки карточки 🏷🎓📱🚨, печать этикетки,
  футер главной). Мобильная адаптация проверена на 390px — горизонтального скролла нет, всё стекается.
- **Качество ответов (RAG):** в ответе — нумерованные карточки-источники (1,2,3) со ссылкой на вещество;
  сноски `[N]` в тексте кликабельны (скролл+подсветка источника); значок верификации цитируемого вещества
  в шапке ответа. В `.env.example` прод-профиль рекомендует `RERANK=1` (кросс-энкодер, top-1 ~89%→92%).
- **Рабочий сценарий «возможно вы имели в виду»:** при отсутствии вещества карточка предлагает близкие
  названия (по префиксу и первому слову) кликабельными чипами — нет тупика для незнакомого пользователя.
  (Печать/экспорт ответа, история, сравнение двух веществ — остались как опциональные, не делал.)
Всё проверено скриншотами, 0 JS-ошибок.

### 27. Расширение данных только из юридически чистых источников (физхимия PubChem)
Контекст: другая сеть предложила приоритезировать ICSC (ILO/WHO) как #1 источник, закрывающий «типовое по
группе». Запрос пользователя — **делать только то, что не требует нарушения/эксплуатации авторских прав.**

**Проверка лицензий до постройки (через research-агентов):**
- **ICSC (ILO/WHO)** — RU-карточки существуют (chemicalsafety.ilo.org, p_lang=ru), но перевод частичный
  (~800–1200 из 1700), доступ через 403/headless, **лицензия «All rights reserved © ILO and WHO», CC только
  для публикаций после 03.05.2023.** Коммерческое использование = запрос на rights@ilo.org. → **ОТКЛОНЕНО** (не берём).
- **IARC (карцерогенность)** — чистого JSON API нет (только PDF Vols 1-123, устар.). Лицензия: *«not for sale
  or for use in conjunction with commercial purposes… require explicit, prior authorization»* (permissions@iarc.who.int).
  Сам факт «вещество = Группа 1» не охраняется, но сводная база — да. → **ОТЛОЖЕНО** на ту же полку, что ICSC.

**Сделано — физико-химические свойства из PubChem (NIH, public domain — безусловно чисто):**
`engine/fetch_physchem.py` (CID по CAS → PUG-View полная запись → парс заголовков Flash Point, Boiling/Melting
Point, Density, Vapor Pressure, Flammable Limits НКПР/ВКПР, Solubility). `engine/physchem.py` (потребитель:
русские подписи + «зачем важно»). `data/physchem.json` {cas:{props:{key:[{value,source}]}}}, прогон по 2029 CAS.
**ПРИНЦИП БЕЗОПАСНОСТИ:** значения НЕ нормализуем и НЕ выбираем «одно верное» — храним СЫРЫЕ строки PubChem
с источником каждого (HSDB/CAMEO/NIOSH/…); несколько значений = разные справочники/единицы, выбор за экспертом,
`needs_review`. Расхождение видит человек, а не тихая подмена. Зачем: темп. вспышки была 29/2597 — а это решение
по пожарной безопасности и хранению (можно ли рядом с огнём, взрывоопасность смеси по НКПР/ВКПР).
Подключено в `/substance` (поле `physchem`) + блок в `substance.html` (проверено скриншотом: этилбензол —
темп. вспышки 12.8 °C/59 °F согласованы, НКПР 0.8% / ВКПР …, давление пара, плотность — все с источниками).
Реиндекс не нужен (рантайм-джойн по CAS, как idlh/pubchem).

**Сделано — экологические ПДК из СанПиН 1.2.3685-21 (офиц. акт РФ, ст. 1259 п.6 ГК — безусловно чисто):**
Источник машиночитаемый верифицирован: DOCX, разбитый на части (airsoft-bit.ru — сторонний хостинг копии
официального текста; нормативная ценность не зависит от носителя). `engine/ingest_sanpin_env.py` парсит:
- **Атмосфера населённых мест** (Часть 1, Табл. 1.1): 721 вещество, 564 с CAS → `data/sanpin_atmo.json`.
  Поля: ПДКмр / ПДКсс (мг/м³), лимит. показатель, класс, флаг «выброс запрещён» (59 веществ).
- **Вода водных объектов** (Часть 3, Табл. 3.13): 1348 веществ, 914 с CAS → `data/sanpin_water.json`.
  Поля: ПДК (мг/л), ЛПВ (с.-т./орг.), класс; пометка «(к)» (канцероген) сохранена в сыром виде.
`engine/sanpin_env.py` — потребитель (джойн по CAS/имени). Подключено в `/substance` (`sanpin_env`) + блок
в `substance.html` (две колонки: атмосфера/вода). **КРИТИЧНО разделено от ПДК рабочей зоны** — это разные
нормативы: у аммиака ПДК р.з. = 20 мг/м³, а ПДК атмосферы = 0,2 мг/м³ (×100!); спутать = ошибка, нота
предупреждает. Пересечение с нашей базой: атмосфера **371**, вода **462** вещества. Проверено скриншотами
(аммиак, бензол — у бензола все три норматива и разный класс по средам: возд. 2 / вода 1).
**ПРИНЦИП БЕЗОПАСНОСТИ:** значения как в акте, десятичная запятая и пометки сохранены сырыми, float только
для одиночных чисел, needs_review; провенанс — реквизиты Постановления ГГСВ РФ № 2 от 28.01.2021.

**Сделано — транспортная идентификация (номер ООН / класс груза) из PubChem (NIH, public domain):**
Гипотеза подтвердилась: номера ООН есть в структурном узле PubChem «Transport Information > DOT ID and Guide»
(не регэксп по тексту — иначе ловили бы номера из аварийных нарративов). `engine/fetch_transport.py` парсит
номер ООН + гид ERG + аннотацию формы + транспортную метку (DOT Label); `engine/transport.py` — потребитель
(+ карта стандартных DOT-меток → классы ДОПОГ: «Flammable Liquid»→«ЛВЖ класс 3» и т.п., не вольный перевод).
`data/transport.json` по CAS. **Было: un_number = 0** (кроме 22 кураторских АХОВ). Стало: CAS-привязка для базы.
**Принцип безопасности:** номер ООН — международный (UN1017 = хлор и в ДОПОГ, и в DOT), языконезависимое число;
несколько номеров = РАЗНЫЕ ФОРМЫ (безводный/раствор 10-35%/смесь) с подписью формы — выбор за экспертом; метку
показываем оригиналом + подсказку класса; needs_review. Подключено: `/emergency` (фолбэк un_number для не-АХОВ +
поле `transport`), `/substance` (`transport`), блок в `substance.html` (формы ООН + метки). Заголовок секции
адаптивный: «АХОВ…» только для реальных АХОВ, иначе «Транспортная идентификация». Проверено скриншотом
(2-(N,N-диэтиламино)этанол: ООН 2686, метка «Corrosive Flammable Liquid» → ЛВЖ кл.3 + Коррозионное кл.8).
Качается фоном параллельно физхимии.

Зависимость: `python-docx` (доустановлен, pure-python, только build-time для СанПиН-DOCX).

### 28. Модуль сверки выдачи (answer-review): человек всегда видит первичку и оценивает ответ
Запрос пользователя: «получил результат на запрос → подтянули документ → помогаем сверить, правильно ли выдача,
правильные ли данные → человек всегда видит первичку и оценивает выдачу и полезность». Это ОТДЕЛЬНЫЙ слой доверия
от верификации данных вещества: там подписывают СТАТИЧНЫЕ ДАННЫЕ («ПДК хлора верен»), здесь — КОНКРЕТНУЮ ВЫДАЧУ
на КОНКРЕТНЫЙ запрос (правильно ли система ДОСТАЛА первичку = retrieval, и СОБРАЛА ответ = extraction, и полезно ли).

**Бэкенд:** `/ask` теперь отдаёт `passages` — точный ТЕКСТ найденного раздела паспорта + провенанс (вещество,
раздел, score, статус верификации). `engine/answer_review.py` (по образцу verification.py): `add()` пишет
подписанную оценку (verdict correct/partial/incorrect + data_ok + useful + пер-источниковые отметки relevant),
`summary()`/`records()`. **Подпись обязательна** — verdict без `by` отклоняется (не «лайк», а ответственная отметка).
Эндпоинты `POST /review`, `GET /reviews`. Хранение `data/answer_reviews.json` (атомарная запись, чистый seed
в поставке — фейков нет, как verification.json).

**Фронт:** в `console.html` под ответом — блок «ПЕРВИЧКА — раздел паспорта (сверьте сами)»: каждый источник
разворачивается в точный текст раздела + ссылка на карточку + кнопки «релевантен / не в тему». Ниже — панель
«Сверка выдачи»: вердикт (верно/частично/неверно), чекбоксы «данные совпадают с первичкой» / «полезно», подпись,
комментарий → `POST /review`. Новый экран `reviews.html` — дашборд качества: % верных, разбивка
retrieval-ошибок («не в тему») vs extraction-ошибок («данные ≠ первичка»), таблица оценок (вопрос/вердикт/кто/когда).
Плитка на главной. Deep-link `?q=...` в консоли (расшаривание/онбординг/скриншоты).

**Почему это ценно:** ловит обе ошибки RAG явно и подотчётно. На скриншоте-демо (lexical) видно силу: запрос
«первая помощь при отравлении хлором» вытащил ХЛОРОФОРМ и ХЛОРОПРЕН вместо хлора — человек, видя первичку, сразу
помечает «не в тему», и дашборд показывает retrieval-проблему. Принцип: **ответ — удобство, паспорт — истина.**
Заодно копится размеченный лог для калибровки порога отказа и эвала. Проверено скриншотами (ответ+сверка,
дашборд с записью и пустой). Тестовая запись сброшена.

### 29. Машинная помощь: уточнение неоднозначности, автодополнение, подтверждение, подсказки
Запрос пользователя: «какие автодополнения или уточнения от машины стоит сделать?». Ключевая идея — заполнить
ПРОПУСК в триаде доверия: было «отказ» (до ответа) и «сверка» (после), но не хватало среднего звена —
**уточнения, когда вход неоднозначен**. Ошибка «хлор→хлороформ» из демо сверки — это и есть неоднозначность входа.

**#1 Уточнение при неоднозначности (главное, безопасность).** `/ask`: если вещество не выбрано и в выдаче 2+
РАЗНЫХ вещества с близким score (разрыв < `CLARIFY_DELTA`, дефолт 0.04) — НЕ генерируем, а возвращаем
`{clarify, candidates}`. Консоль показывает «Уточните вещество» с чипами (вещество + %). Клик → re-ask с
`substance` (жёсткий скоуп: выдача только по выбранному веществу, ключ кэша отдельный). Принцип: цена ошибки =
здоровье, лучше спросить, чем смешать ответ из разных паспортов. Проверено: запрос про «хлор» даёт чипы
трихлорметан/хлоропрен (lexical), а не молчаливый неверный ответ.

**#2 Автодополнение названий.** Консоль: кастомный дропдаун из `/substances/names` (подстрока + синонимы +
приоритет префикса), навигация ↑↓/Enter/Esc, подсветка совпадения, дебаунс 170мс. Карточка вещества: уже было
через нативный `<datalist>`. Слой ПРЕДОТВРАЩЕНИЯ — «хлор» сразу виден в подсказках, опечаток/«не найдено» меньше.

**#3 Мягкое подтверждение вещества.** На каждом ответе: «Понял запрос как про <вещество> (релевантность X%).
Не то? [альтернативы]». Альтернативы — те же distinct-кандидаты выдачи; клик переспрашивает по выбранному. Прозрачность распознавания без блокировки.

**#4 Подсказки уточнения вопроса.** После ответа чипы «первая помощь · СИЗ · хранение · разлив · пожар · знаки
опасности» → подставляют шаблон + текущее вещество и переспрашивают. Ведут к более точному запросу (разделы паспорта).

Бэкенд: `Ask.substance`, `CLARIFY_DELTA`, `_distinct_candidates()`, поля `candidates`/`chosen`/`clarify` в `/ask`.
Фронт: `console.html` (renderClarify, recog, refine, автодополнение с клав-навигацией), deep-link `?q=`. Всё
проверено скриншотами (уточнение, полный стек подтверждение+первичка+подсказки+сверка, дропдаун автодополнения).

### 30. Полная документация системы (`docs/О-СИСТЕМЕ.md`)
По запросу «нужен полный рассказ на техническом и не техническом языках». Один связный документ (~2700 слов,
17 разделов): что это за система и чем не является; ценность по ролям; как работает (простым языком + RAG-схема);
почему ИИ и почему «ИИ не оракул»; три слоя доверия; машинная помощь; данные и их честность (источники, юр.
чистота, почему ICSC/IARC отклонены); отстройка; внедрение; кейсы; КИИ/152-ФЗ; **переносимость шаблона на
другие задачи** (юр./мед./комплаенс/ТО); факты и цифры (снимок, числа не выдуманы); честные ограничения; роадмап; FAQ.
Все цифры — из текущего состояния системы (тот же принцип «не выдумывать», что в продукте).

Дополнительно по запросу владельца — два внутренних документа:
- `docs/ВЛАДЕЛЬЦУ-полная-дока.md` — рабочий справочник владельца: карта репозитория, все ~40 модулей по
  группам, эндпоинты, env-профили, данные, как запустить (демо/прод/docker), демо-сценарий, **паттерн
  добавления чистого слоя данных**, святые правила (не выдумывать; verification/answer_reviews чистые в
  поставке; юр. чистота; офлайн-рантайм), «где зарыты тела» (lexical vs semantic, фоновые фетчи, CAS-less,
  скриншоты через Chromium, два слоя уверенности), чеклист перед показом/поставкой.
- `docs/ПРЕЗЕНТАЦИЯ-заметки.md` — talk track спикера: аудитории→акценты, структура выступления с дословными
  формулировками, точный демо-сценарий, ответы на возражения (галлюцинации/КИИ/vs ChatGPT/железо/лицензии/
  замена инженера), сильные one-liners, чего НЕ обещать, тайминги (60с/20мин/45мин), call to action.

### 31. Домен для предпринимателя (`docs/ДОМЕН-для-предпринимателя.md`)
По запросу «не описано для меня что это вообще за домен химия — не для химика, а для предпринимателя».
Праймер по домену деловым языком (11 разделов): картина мира за минуту; что такое паспорт безопасности/SDS
(техпаспорт на опасное вещество, 16 стандартных разделов по ГОСТ 30333 → продукт делается раз и работает
на любом заводе); словарь из ~8 терминов (ПДК / класс 1–4 / АХОВ / IDLH / СИЗ / номер ООН / СГС / ПДК
раб.зоны ≠ нас.мест); кто на заводе мучается (инженер ОТ/ПБ, аварийка, эколог, директор, мастер — боли);
почему за это платят (регуляторное принуждение Ростехнадзор 116-ФЗ / Роспотребнадзор, цена инцидента, цена
простоя — **суммы штрафов помечены «уточнить», не выдуманы**); почему сейчас (импортозамещение, запрет облака
по КИИ/152-ФЗ, господдержка, зрелость локальных моделей); почему можно продавать не будучи химиком (продаёшь
доступ, химию подтверждает их эксперт by design); рынок без выдуманных цифр (рамка «как считать» из реестра
ОПО); переносимость ядра на смежные домены; скрипт ответа «а ты в химии шаришь?»; домашка основателя.

### 32. Приведение системы к ГОСТам и ФЗ (4 направления сразу)
По запросу «изучи госты и фз — может сразу приведём систему к ним». Изучил нормативку по открытым
источникам (docs.cntd.ru, base.garant.ru). **Ключевая находка:** ГОСТ 30333-2007 заменён
**ГОСТ 30333-2022** (введён 2023, гармонизация с СГС/GHS Rev.7) — система ссылалась на устаревший. Сделано:

- **Реестр нормативки** `normative.py` (+ `/normative`): 13 актов (ГОСТ/СанПиН/РД/ФЗ) с версией и статусом
  (current/superseded/verify/historic), `applicable_for()` — какие действующие акты применимы к веществу
  (раздел 15 паспорта). Единый источник правды по редакциям. Факты выверены, где сомнение — `verify`.
- **Карта 16 разделов ГОСТ 30333** `gost_sections.py`: `coverage_for(out)` инспектирует собранную карточку
  и говорит, чем закрыт каждый из 16 разделов (свой слой / частично-типовое / только корпус / пробел) с
  провенансом. В каждый `/substance` добавлено поле `gost_map`; агрегат `/gost/summary` по всей базе.
  Формулировки названий помечены `title_needs_review` — сверить с оригиналом PDF (структура = стандарт СГС).
- **Аудит версий** `audit_versions.py` (+ `data/version_audit.json`): сканит код/доки/данные, ловит ссылки
  на устаревшие редакции по реестру. Нашёл 14 → обновил авторские места (corpus.py, README, О-СИСТЕМЕ,
  _meta корпусов: 30333-2007→2022). Осталось 7 — все в сыром источнике `gn.htm.html` (не утверждения системы).
- **ФЗ-контур** `/compliance/selfcheck`: само-проверка позы под 152/187/116-ФЗ (офлайн-рантайм, X-API-Key,
  append-only аудит-лог на диске, провенанс, легальность источников, актуальность нормативки). Демо: 5/6
  (ключ доступа в демо-режиме выкл. — показывается честно). Контур во многом уже был (hygiene.audit→jsonl).
- **UI:** новая страница-дашборд `gost.html` (полосы покрытия 16 разделов + ФЗ-контур + реестр версий с
  подсветкой устаревшего), инлайн-блок «Соответствие ГОСТ 30333» в карточке вещества (сетка 16 разделов с
  цветными статусами), плитка на главной. Проверено скриншотами (Chromium).
- **Документ** `docs/СООТВЕТСТВИЕ-ГОСТ-ФЗ.md`: реестр актов, gap-матрица 16 разделов с живыми цифрами,
  ФЗ-контур (требование→как закрыто), честные пробелы и дорожная карта (раздел 13 «отходы» — полный пробел,
  кандидат №1: ФККО Росприроднадзора). Снимок: 2597 веществ, 16/16 разделов в выдаче, раздел 13 — 0%.

### 33. Safety-фикс: точное имя вещества в /ask не якорилось (хлор → «хлор диоксид»)
Найдено при ревью (пользователь ввёл «хлор», система не уточнила). Диагностика по фактам:
`/substance/хлор` резолвит верно (Cl₂, CAS 7782-50-5), а `/ask "хлор"` шёл чистым retrieval и
возвращал **«хлор диоксид» (ClO₂, 0.637)** — ДРУГОЕ вещество с другой опасностью; уточнение не
срабатывало (вернулся один кандидат). В базе **524 вещества** содержат «хлор» в имени.
**Причина:** `/ask` (в отличие от `/substance`) не якорил точное совпадение запроса с именем.
**Фикс** (`service.py /ask`): если ВЕСЬ запрос точно резолвится в имя/синоним вещества (`_resolve_sub`) —
якорим scope на него (`chosen`, новый флаг `auto_chosen`), как делает `/substance`. `_resolve_sub` —
строго точное совпадение, поэтому вопросы-предложения («первая помощь при отравлении хлором») НЕ
затрагиваются. Проверено: «хлор»→хлор (Cl₂, 0.677); «аммиак»→аммиак (1.078); предложение про хлор →
по-прежнему clarify (хлороформ/хлоропрен — защитная сетка). Консоль показывает «Понял запрос как про
хлор» (мягкое подтверждение). Скриншоты сняты. Это safety-критично: хлор ≠ диоксид хлора.

### 34. QA-прогон в браузере: узкие места ретривера + митигации + проверка семантикой
По запросу «сам пройди тест-кейсы — может найдёшь узкие места». Прогон 30+ запросов через живой
сервис (те же вызовы, что UI) + скриншоты. **Playwright MCP в сессии не подключён** — добавлен
`.mcp.json` (@playwright/mcp) для будущего интерактивного прогона; пока — API + headless Chromium.

**Найденные узкие места (все — артефакты лексики):**
1. Ложный отказ на естественном вопросе: «как хранить серную кислоту» → ОТКАЗ (score 0.377), хотя
   «серная кислота хранение» → 0.985. Лексика ломается на склонениях («серн**ую** кислот**у**») и
   вопросных словах.
2. Общее слово → произвольное (часто опасное) вещество: «водород»→гидробромид (HBr, 0.877),
   «натрий»→натрий цианат, «оксид»→оксид углерода. Один уверенный неверный кандидат → clarify не
   срабатывал (нужно 2+).
3. Clarify предлагал дубликаты: «ацетон» и «пропан-2-он (ацетон)» — одно вещество (CAS 67-64-1).
4. Clarify с неверным фаворитом: «температура вспышки бензола» → этилацетат первым.

**Проверка семантикой (RETRIEVER=semantic, bge-m3, Ollama) — side-by-side на тех же кейсах:**
семантика чинит почти всё. «как хранить серную кислоту» → CLARIFY(серная кислота, серный ангидрид);
«первая помощь при отравлении хлором» → CLARIFY(**хлор**, хлороводород) вместо хлороформ/хлоропрен;
«температура вспышки бензола» → бензол прямо; «водород» → честный ОТКАЗ вместо HBr. Это доказывает
тезис дорожной карты: узкие места — лексические, bge-m3 их снимает. Нюанс: порог отказа 0.62
калиброван под лексику, у семантики скоры ниже (аммиак 0.657) — нужна своя калибровка (~0.50).

**Митигации в лексике (дефолтный офлайн-режим), `service.py` + `console.html`:**
- `_too_generic()` + гард в `/ask`: одиночный общий токен (не точное имя, подстрока в ≥8 веществах) →
  clarify «слишком общий запрос» с подсказками вместо молчаливого ответа про опасное вещество.
  «водород» → подсказки сероводород/хлороводород/перекись водорода…; «хлор/аммиак» якорь не задет.
- `_distinct_candidates()` дедуп по CAS: «ацетон»+«пропан-2-он (ацетон)» больше не дублируются в clarify.
- `renderClarify` показывает причину «слишком общий» и скрывает «0%» при отсутствии score.
Проверено: точные имена (7/7) и отказ на вне-теме (4/4) не сломаны; скриншоты сняты.
Остаётся (лечит семантика, в лексике отложено): ложный отказ #1 на склонённых фразах.

### 35. Раздел 13 «Удаление отходов» — закрыт последний полный пробел (`waste.py`)
Кандидат №1 из gap-анализа. Slой типовых мер обращения с отходами по группе вещества (как baseline
для первой помощи) + рамка норматива. **Safety:** ФККО не маппится по CAS (каталог по типу/
происхождению отхода) — конкретный код и класс отхода НЕ выдумываем, их присваивает эколог.
- `waste.py`: `GROUP_WASTE` (меры по 10 группам: кислоты/щёлочи/окислители/ЛВЖ/активные металлы/
  токс.газы/цианиды/cl_release/орг.растворители/пероксиды), `for_substance()` через `compat.classify`,
  `FRAMEWORK` (класс отхода I–V по Приказу Минприроды № 536; код ФККО — эколог; 89-ФЗ).
- Нормативка выверена по открытым источникам: **89-ФЗ от 24.06.1998**, **Приказ Минприроды № 536 от
  04.12.2014** (критерии классов отхода I–V, зарег. Минюст 29.12.2015 № 40330), **ФККО** (Росприроднадзор).
  Добавлены в `normative.py` (+ в `applicable_for` при наличии waste). **Класс отхода I–V ≠ класс
  вещества 1–4** — подчёркнуто в UI (как ПДК раб.зоны ≠ нас.мест).
- Wired: `out["waste"]` в `/substance`; раздел 13 в `gost_sections` теперь `partial` (был `gap`);
  блок «Удаление отходов» в `substance.html` (меры + рамка + оговорка).
- Результат: **GOST 16/16 разделов закрыты структурно, полных пробелов 0** (серная кислота: 8 full +
  8 partial). Агрегат: раздел 13 partial=2597 (был gap=2597). Проверено скриншотом.
- Доки обновлены: `СООТВЕТСТВИЕ-ГОСТ-ФЗ.md` (раздел 13 не пробел, roadmap ✅), README.

### 36. Дозаполнение данных параллельно: эко-ПДК расширены (ОБУВ/ОДУ)
По запросу «продолжим по данным, параллельно». Сначала проверка: **physchem и transport уже на 100%
завершены** (0 непокрытых CAS из 2029; 1219 с физхимией, 388 с ООН — это реальный потолок PubChem,
дофетчивать нечего). Реальный рычаг — эко-ПДК: парсились только ПДК-таблицы.
- Расширен `ingest_sanpin_env.py`: добавлен парсинг **ОБУВ атмосферы** (Часть 1, табл.[1], 1728 строк)
  и **ОДУ воды** (Часть 3, табл.[13], 434 строки) — ориентировочные уровни для веществ без ПДК.
  Дискриминатор `norm_type` (ПДК/ОБУВ/ОДУ); ОБУВ/ОДУ добавляются ТОЛЬКО где ПДК нет (ПДК приоритетнее).
- **Safety:** ОБУВ/ОДУ ≠ ПДК (слабее по статусу) — помечено отдельным бейджем «ориентир.» в UI и в note,
  не смешивается с ПДК. Источник — тот же официальный акт (ст. 1259 п.6 ГК), needs_review.
- `sanpin_env.py` консьюмер: `atmo_for`/`water_for` отдают norm_type + obuv/odu; UI (`substance.html`)
  показывает бейдж ОБУВ/ОДУ.
- Результат: **атмо с CAS 565 → 1712** (ПДК 721 + ОБУВ 1728), **вода 915 → 1146** (+ОДУ 434).
  Раздел 12 (эко-ПДК) по базе: **full 686 → 1107** (+421 вещество). Проверено скриншотом (алюминий нитрид → ОБУВ 0,01).

### 37. Узкие места ретривера закрыты + сверка редакций/формулировок ГОСТ
По запросу «фигачь всё параллельно». Две части: (а) чинка найденных в QA узких мест лексики,
(б) применение результатов двух фоновых ресёрч-агентов (сверка по первоисточникам).

**(а) Ретривер — устойчивость к склонению и ложным совпадениям (`service.py`):**
- Переписан детектор упоминания вещества `_mention_scope`: вместо 4-символьного префикса —
  совпадение по **общему корню с поправкой на склонение** (`_root_match`: общий префикс ≥4 И
  «хвост» за корнем ≤3 у ОБОИХ слов). Это граница между склонением и другим веществом:
  «бензола»→**бензол** (а не 3-бензилметилбензол), «хлором»→**хлор** (а не хлороформ/хлоропрен),
  «серную кислоту»→**серная кислота**, «азотной кислоты»→**азотная кислота**. Короткие имена
  (хлор, азот, len≥4) теперь тоже участвуют. Ранжирование по силе совпадения, ничья → None (уточнение).
- Гейт отказа: если вещество **зафиксировано** (выбрано/заякорено/упомянуто) — абсолютный порог
  снимается (выдача строго из его паспорта, человек видит первичку). Чинит ложный отказ на
  «как хранить серную кислоту» (score 0.33 → теперь ОТВЕТ, раздел 7). НЕ подменяем другим
  веществом: при locked берём большой пул и фильтруем по выбранному; нет фрагментов → честный отказ.
- Регрессионный прогон 10 кейсов: все маршрутизируются верно (хранение→р.7, первая помощь→р.4,
  физхимия→р.9), общие слова («водород») → CLARIFY, безвеществные → ОТКАЗ. Цена ошибки=здоровье соблюдена.

**(б) Сверка по первоисточникам (2 агента, ≥2 источника каждый):**
- **Формулировки 16 разделов** = точные цитаты п. 5.1 **ГОСТ 30333-2022** (allgosts.ru + meganorm.ru,
  полный текст). Уточнены разделы **1** (…об изготовителе и/или уполномоченном изготовителем лице),
  **6** (…аварийных **и чрезвычайных** ситуаций), **7** (…**при погрузочно-разгрузочных работах**),
  **15** (…законодательств**ах**), **2** (опасности(ей)). `TITLE_NEEDS_REVIEW=False`, баннер в UI снят.
- **ГН 2.2.5.3532-18** — **утратил силу 01.03.2021** (пост. Гл. санврача № 2 от 28.01.2021), поглощён
  СанПиН 1.2.3685-21. Статус в `normative.py`: `verify` → **`superseded`** (replaced_by). by_status: superseded 2→3.
- **РД 52.04.253-90** — **формально действует** (письмо МЧС № 48-528-14 от 03.10.2017); спец. замены
  для зон заражения АХОВ нет; параллельно — Прил. Б (обяз.) к СП 165.1325800.2014. Note уточнён.
- Перегенерирован `version_audit.json`; обновлены `СООТВЕТСТВИЕ-ГОСТ-ФЗ.md` (реестр + roadmap 6,7 ✅),
  `gost.html` (лид). Дашборд проверен скриншотом.

### 38. UI «для человека далёкого от IT»: чистка, упрощение, тёплый стиль (через Playwright MCP)
По запросу «отсмотри весь UI и сделай его доступным». Прогон и проверка — через Playwright MCP
(десктоп + мобайл 390px). Аудитория «все сразу»: оператору просто, специалисту глубоко (разнесли по экранам).
- **Убрана IT-утечка №1:** поле «адрес системы: http://127.0.0.1:8000» удалено со всех 12 экранов
  (адрес и так берётся из `location.origin`; оставлен скрытый input для fallback при открытии файла).
- **Починены битые ссылки футера:** `MARKET.md`/`DEMO.md`/`WORKLOG.md` давали 404. Добавлен бэкенд-роут
  `GET /doc/{name}` — рендерит любой `.md` из корня и `docs/` в стилизованную читаемую страницу (markdown→HTML),
  `/doc/` в auth-exempt. Футер теперь ведёт на реальные доки.
- **Главная переделана:** вместо «стены» из 12 равнозначных плиток — 3 крупных действия оператора
  (Спросить / Узнать о веществе / Авария) + свёрнутый `<details>` «Для инженеров ОТ/ПБ и руководства».
- **Тёплый стиль на всех экранах:** единый сдвиг палитры (значения CSS-токенов одинаковы во всех файлах) —
  bg/panel/line теплее, txt контрастнее, muted ярче (доступность), accent #3b82f6→#5b9dff, orange теплее;
  базовый шрифт 15→16px. Дружелюбнее «терминального» вида, вёрстка не тронута.
- **Формулы с индексами:** хелпер `subF()` (убирает лишний пробел, оборачивает цифры в `<sub>`) —
  `H2SO4`→H₂SO₄, `NH3`→NH₃, `Сl 2`→Cl₂. В substance/label/registry.
- **Чистка жаргона:** «релевантность»→«совпадение с запросом», «первичка»→«текст из паспорта»,
  утечка `needs_review` в видимом тексте (emergency/substance)→«Требует проверки экспертом»,
  «retrieval»→«поиск» (reviews), «провенанс»→«указание источника» (gost).
- **Проверка:** home (десктоп/мобайл/раскрытие), substance (H₂SO₄), console (ответ: «совпадение 95%»,
  без «релевантность»/«первичка»), gost (бары/таблица), emergency, registry, рендер `/doc/`. Лог чистый, 0 ошибок.
- **Смягчён «терминальный» вид:** статус-строка `.status` и back-ссылка `.back` переведены с моноширинного
  шрифта на обычный во всех 10 экранах (убрана разрядка «система  на  связи»). Цифры/CAS/H-коды/бейджи
  оставлены моноширинными (точность и табличное выравнивание). Удалены мёртвые `index.html`/`index_pro.html`.

### 39. Системный QA-проход вёрстки: «поехавшую» вёрстку выловили и починили (Playwright)
По запросу «много кривого хреново сверстанного». Метод: программная проверка переполнения
(`document.scrollWidth` vs `innerWidth` при 390px) на каждом экране + поиск элементов, выходящих за вьюпорт.
Найдено и устранено 5 дефектов:
- **`.pct` число+подпись слипались** (readiness/reviews): «0%готовности» в одну строку. Причина —
  селектор `.pct small` бил по вложенному элементу, а `<small>` был *соседом* `<span class=pct>`.
  Заменено на `.pct+small{display:block…}` — теперь «0%» крупно, подпись отдельной строкой под ним.
- **gost.html — горизонтальный скролл на мобайле** (scrollW 437>390): (1) строки карты разделов
  (`.nm` 215px + `.bar` 120px + `.pc`) — добавлен `@media(max-width:560px)`: имя `flex:1;min-width:0`,
  бар 84px; (2) таблица реестра нормативки (399px) распирала страницу — `#reg{overflow-x:auto}`,
  скролл теперь внутри блока, а не у всей страницы.
- **guide.html — горизонтальный скролл** (scrollW 555>390): подсказки `.tip` были `display:flex`,
  из-за чего инлайновые `<b>красный/жёлтый/зелёный</b>` становились отдельными flex-элементами и не
  переносились. Переведено на `display:block` с `float:left` у иконки — текст обтекает и переносится.
  Примеры `.ex .io` получили `flex-wrap:wrap` + `min-width:0`/`overflow-wrap` у `.res`/`code`.
- **substance.html — физ-хим блок** (`.physchem`): источник прижат к значению, тесно. Источник вынесен
  на отдельную строку (`display:block`), увеличены интервалы/шрифт, `overflow-wrap:anywhere`.
  (Поправка к этому пункту: поле `physchem` ВСЁ-ТАКИ заполнено — тянется по CAS из `data/physchem.json`,
  отдельно от substances_clean.json. См. пункт 40 — там это вскрылось и разобрано.)
- **reviews.html — остатки жаргона:** «retrieval»/«extraction» в скобках, «первичка»→«текст из паспорта»,
  «данные ≠ первичка»→«данные ≠ паспорт», заголовок/лид переписаны простыми словами.
- **Проверка:** все 4 экрана — 0 переполнения при 390px и 1280px; readiness/reviews — подпись под числом;
  gost — бары и таблица в рамках; guide — подсказки/примеры переносятся (десктоп тоже чистый);
  substance (хлор) — карточка рендерится без переполнения, формула Cl₂ с индексом.

### 40. Локализация физ-химии + чистка «тофу»-иконок (по жалобе «кривая верстка»)
Пользователь показал «кривую вёрстку». Браузерный обход вскрыл, что худшая секция — «Физико-химические
свойства» в карточке вещества: рваный дамп сырого английского (°F/mmHg/atm), по 5-6 дублей на свойство,
и в источниках — **ICSC ILO-WHO** (защищён АП ВОЗ/ILO, не public domain).
- **Решение владельца:** пилот некоммерческий, чужой труд не перепродаём → ICSC НЕ вырезаем, оставляем
  с провенансом + needs_review и **локализуем**. На проде завод подставит свои данные. (См. [[rag-sds-safety-rule]].)
- **`engine/physchem_l10n.py` (новый):** локализатор описаний по выверенному словарю фраз/терминов.
  ГРАНИЦА БЕЗОПАСНОСТИ — числа и единицы НЕ пересчитываем (°F не в °C; баг пересчёта = «цена ошибки»),
  переводим только словесные описания (растворимость, горючесть, состояние). Метки-источники «(NTP, 1992)»
  сохраняем. Грамматика: «miscible with X»→«неограниченно растворим в X» (предложный, без кривого «с этаноле»).
  «at»→«при» глобально перед числом. Навигационный мусор HSDB («please visit…») выкидываем. Покрытие:
  из 12 082 англ. значений ~74% полностью на русском, остаток (редкие сложные строки) — частично, needs_review.
- **`engine/physchem.py`:** применяет локализатор + **дедуп** значений с объединением источников
  (6 температур кипения из 6 справочников → 2-3 уникальных значения, источники слиты). «Максимум» сохранён —
  данные не теряются. Форма values: `[{value, sources:[...]}]`; обновлён рендер в substance.html.
- **«Тофу»-иконки → inline-SVG:** `🜂` (огонь, заголовок физ-хим) `📖` (футер главной) `📥` (CSV в реестре) —
  цветные/обскурные эмодзи давали чёрный квадрат на system-ui. Заменены на stroke-SVG (как карточки главной).
  Моно-символы (⚠ ⚑ ✓ ✕ ◐ ♺ ✚) оставлены — рисуются системным шрифтом надёжно.
- **Проверка:** перезапуск сервиса; карточка хлора — секция физ-хим читаема по-русски, источники слиты,
  SVG-огонёк вместо квадрата; главная — книжная иконка в футере без «тофу»; 0 переполнения.

### 41. Синхронизация документации и презентационных материалов с текущим состоянием
По запросу «отразить архитектуру/принципы и обновить все презентационные материалы». Сверил факты с кодом
и данными (не на память), привёл доки к единому виду.
- **Зафиксированы истинные числа** (через `for_cas`, не наугад): вещества 2597, чанки 25 970, GHS 1 493,
  IDLH 207, физхимия **1 254**, ООН **408**, синонимы 146, UI-экранов **12** (было 13 — `index`/`index_pro`
  удалены), эндпоинтов ~37. Поправлены устаревшие «~860 физхимия» и «13 экранов» во всех доках.
- **`docs/О-СИСТЕМЕ.md`** (главный «рассказ»): в раздел 8 добавлен принцип «локализуем описания, но НЕ
  пересчитываем числа/единицы (пересчёт = вычисление = цена ошибки)»; юр-чистота переписана на **двухслойную**
  (ядро — public domain + офиц. акты РФ; слой обогащения физ-химии — агрегат PubChem с ICSC, на некоммерч.
  пилоте с провенансом, на проде замещается паспортами завода); физхимия описана как агрегат справочников,
  локализованный; обновлены «Факты и цифры» и упоминание UI-для-неайтишника в архитектуре.
- **`docs/ПРЕЗЕНТАЦИЯ-заметки.md`**: ответ на возражение «это легально?» переписан на двухслойную формулировку;
  добавлено возражение «почему данные на английском/в странных единицах?» с готовым ответом про локализацию.
- **`docs/ВЛАДЕЛЬЦУ-полная-дока.md`**: 13→12 экранов (список экранов выправлен: убраны мёртвые index*,
  добавлен `gost`); блок про лицензии переписан под решение по ICSC; в «святые правила» добавлен принцип
  про локализацию без пересчёта единиц.
- **`README.md`, `docs/СООТВЕТСТВИЕ-ГОСТ-ФЗ.md`**: позиция по источникам приведена к двухслойной; физхимия
  отмечена как локализованная; число ООН 408 (совпало с верификацией).
- **`DEMO.md`**: починены битые инструкции акта-0 — ссылка на удалённый `index_pro.html` (офлайн-фоллбэк) и
  на поле «сервис», которого больше нет в UI (адрес берётся из `location.origin`).
- **Проверка:** все доки отдаются через `/doc/*.md` (200), обновлённый текст на месте. Связано с
  [[rag-sds-safety-rule]] (решение по ICSC).
- **Новый док `docs/АРХИТЕКТУРА.md`** — визуальная карта системы: 4 ASCII-схемы (контур air-gap / build-time
  vs runtime; поток RAG-запроса с гейтами отказа и уточнения; слои данных и джойн по CAS; три слоя доверия)
  + карта модулей и стек — точные по коду (список модулей/эндпоинтов сверен). Без CDN/Mermaid (продукт
  офлайн). Слинкован из О-СИСТЕМЕ (раздел 4.2 + футер) и из футера главной (пункт «Архитектура»).
  В шаблоне `/doc` (`service.py`) шрифт code-блоков уменьшен до 11px (`white-space:pre`), чтобы широкие
  ASCII-диаграммы влезали в колонку без горизонтального скролла — проверено в браузере (overflow 0).
- **Актуализация бизнес-материалов под статус пилота:** `MARKET.md` раздел 5 («что перенять у рынка»)
  переписан со статусами — из 8 «ближних» функций конкурентов **4 реализованы** (QR-этикетки `/qr`, реестр
  площадок `registry`, этикетки СГС, квиз), 2 частично (версии НПА есть/diff паспортов нет; CSV-экспорт в
  1С/полная синхронизация нет), 2 в roadmap — сверено по коду/эндпоинтам. Раздел 4 усилен отстройками
  (отказ вместо галлюцинации, честность данных как фича). `docs/ДОМЕН-для-предпринимателя.md`: добавлена
  заметка «продукт уже рабочий, не концепт», починен англицизм-глюк, добавлены ссылки на АРХИТЕКТУРА/MARKET.

### 42. Имя и бренд — HEFEST
Пилот назван **HEFEST** (Гефест, бог-кузнец: из хаоса PDF-паспортов куётся проверяемая карточка
вещества). Выбор после перебора: отвергнуты Talos (созвучие «фаллос» + не про химию), Aegis/Aletheia/
Hygeia/Pharos и русские (Допуск/Сверка/Цитата/Эгида). Обоснование и формы — в `BRAND.md`.
- **Знак** наковальня+искра, монохром `#f9a23b` (совпал с акцентом UI): `assets/brand/hefest-mark.svg`,
  `hefest-lockup.svg`. Инлайн в шапке `home.html` (знак + вордмарк HEFEST + дескриптор для неайтишника).
- `<title>` всех 12 экранов → суффикс `· HEFEST`. Заголовки README/О-СИСТЕМЕ/ВЛАДЕЛЬЦУ, FastAPI title.
- Проверено в браузере (DOM-замеры; пиксельный скрин блокирует таймаут харнесса): 1280px и 390px —
  знак рендерится (2 пути), переполнения нет, шапка чисто стекается на мобайле. Имя репозитория — `rag-sds`.

### Git — репозиторий инициализирован
`git init`; базлайн на `main` (108 файлов), работа сессии — ветка `feature/l10n-physchem-ui-docs`
тематическими коммитами (локализация / UI / сервис / доки / бренд). `.gitignore`: исключены `*.npy`
(~300M), Qdrant-индекс (~275M), `.bak`, кэши, рантайм-логи, тяжёлые PDF/HTML-справочники, QA-артефакты,
`.env`, `.claude/`. Ремоута нет, ничего не пушилось.

### Окружение / зависимости (доустановлено)
- `rank_bm25` (был в requirements, отсутствовал — падал импорт retriever).
- `sentence-transformers` (импорт ломается на torchcodec — НЕ используем; реранкер на `transformers`).
- Доступно: numpy 2.4.3, sklearn 1.9.0, fastapi 0.136, torch 2.8.0 (MPS), transformers 4.57.6.

### 9. Зонд PubChem (данные вширь — синонимы)
`feasibility`: PubChem PUG REST доступен (синонимы по CAS), НО выдаёт в основном **английские**
имена (Oxirane, Ethylene Oxide…), русских (этиленоксид) почти нет. Для русскоязычного retrieval
ценность маргинальная. Вывод: синонимы курировать вручную (рус.) или брать **АХОВ-карточки**,
а не лить PubChem. Полную заливку НЕ делал (низкий ROI).

### Дорожная карта (статус)
1. ✅ Семантика в сервис · 2. ✅ Grounded-генерация · 3. ✅ Реранкер (кросс-энкодер 92%)
4. ⚠️ Синонимы PubChem — зонд: англоцентрично, отложено (лучше рус.-курация/АХОВ)
5. ✅ АХОВ-карточки (22 вещества: ООН-номер, ДОПОГ, /emergency) — needs_review, сверить
6. ✅ OCR-пайплайн (PyMuPDF+easyocr, скан→текст с шумом на MPS) — Ступень-1 доказана
7. ✅ Qdrant (локальный режим, паритет 6/6; прод — Docker)
8. ✅ Расширение eval (47 вопросов, top-1 89%) · clean-база сравнение — считается

---

### 43. Расширение инструментов для завода: 5 новых модулей (ветка `feature/plant-tools-expansion`)
По итогам разведки рынка/болей (эколог, ОТ/ПБ, аварийная служба, директор) и **разведки по
коду** (что реально есть в репо, а не из веба) собраны 5 изолированных модулей — каждый свой
`engine/*.py` + свой HTML-экран, общая регистрация эндпоинтов и навигация — вручную, со сверкой
святого правила. Сборку вели 5 параллельных агентов, интеграцию и проверку — руками.
- **СИЗ по веществу** — `engine/ppe.py` + `ppe.html`, `GET /ppe/{name}`. Паспортный `ppe` (grade=passport)
  или типовое из `baseline.py` (grade=baseline + дисклеймер). Раскладка по категориям — только показ.
- **Карта склада** — `engine/warehouse.py` + `warehouse.html` + `data/warehouse_zones.json` (demo-шаблон,
  `is_template`), `GET /warehouse/{plant}`. Зональный аудит через `compat.check`; пары помечены
  `structured` (2% паспортных) vs `эвристика по группе`. Конфликты подсвечены.
- **Режим инспектора** — `engine/inspection.py` + `inspection.html`, `GET /inspection/{plant}`. 7 метрик
  готовности из НАШИХ данных (ПДК/класс/СГС/верификация/совместимость/актуальность НПА/разделы ГОСТ),
  **не** официальный проверочный лист — постоянная плашка-дисклеймер. ПЛОЩАДКА: 74–82%.
- **Класс отхода (предв.)** — `engine/waste_calc.py` + `waste.html`, `POST /waste/estimate`. Формула 536;
  честный honest-gap: при нехватке показателей (ЛД50=0, ПДКп 44%, нет таблицы баллов 536) возвращает
  `class=null / insufficient_data / needs_review` + 6 блокеров. Код ФККО — за экологом, не выдумываем.
- **Карточка диспетчера** — `engine/dispatch.py` + `dispatch.html`, `GET /dispatch/{name}`. Зона из
  `rd_zone` (РД 52.04.253-90, без округления), действия из `/emergency`, плотность — только при
  подтверждённых физсвойствах, телефоны ПАСФ/ЕДДС — плейсхолдеры предприятия. Хлор 1000 кг → 1.75 км.
- **Интеграция**: `service.py` — импорт 5 модулей, whitelist `_UI_PAGES`, 5 эндпоинтов; `home.html` —
  5 плиток в разделе «для специалистов». Все `/health` 200, экраны рендерятся (headless Chrome),
  самопроверки модулей зелёные. Не коммичено/не пушено (ждёт просьбы). Инструктажи сознательно НЕ брали
  (рынок занят 1С, электронные журналы юридически = бумага).
- **Демо-полировка (5 параллельных агентов, по экрану каждый)**: автозагрузка по URL-параметру
  (`?name=` / `?plant=` / `?mass_kg=`) + чипы-пресеты, чтобы экран открывался сразу с данными, а не
  пустой формой. СИЗ → хлор; диспетчер → хлор 1000 кг (чипы валидных АХОВ из `/zone/substances`);
  инспектор → ПЛОЩАДКА 81.9% (3 чипа площадок); склад → ПЛОЩАДКА (3 FORBIDDEN в зоне C); отходы → пример
  состава, «класс не определён» подан как осознанная честность (+блок «почему» и плашка про эколога).
  Дисклеймеры (надзор/методика/ФККО) сохранены. Проверено headless-Chrome: все 5 рендерятся с данными.
- **Кросс-ссылки (3 параллельных агента)**: продукт связан в одно целое — карточка вещества
  (`substance.html`) → «Подбор СИЗ» / «Карточка диспетчера»; аварийная карточка (`emergency.html`) →
  «развернуть оперативную карточку диспетчера»; реестр завода (`registry.html`) → «Карта склада» /
  «Режим инспектора» (+ сам читает `?plant=` для перехода туда-обратно). Все ссылки — deep-link с
  `encodeURIComponent`, подтверждено в rendered DOM (кириллица кодируется). Святое правило — только
  навигация, контент не трогали. `humanizer-ru` по экранам НЕ гоняли (нормативка/дисклеймеры).
- **Мобильный QA (Playwright `getBoundingClientRect` на 390px, не пиксельный скрин)**: прогнал все 5 новых
  + 3 hub-экрана на горизонтальное переполнение. Найдено одно: на `inspection.html` таблица метрик
  вылезала на 23px → починено мобильной медиа-запросой (`table-layout:fixed`, фикс-колонки покрытия 60px /
  статуса 96px, перенос меток и бейджей). Остальные чисты; таблица реестра скроллится внутри контейнера
  (существующее поведение, страница не переполняется).
- **Security (авто-ревью)**: флаг XSS — неполный экранировщик в атрибутном контексте на `inspection.html`
  (`esc` крыл только `&<>`, а подставлялся в `value=`/`data-plant=`/`class=`). Данные курируемые
  (имена заводов, статусы ok/partial/gap), эксплуатация маловероятна, но починено defense-in-depth:
  `esc` → полный (`"`+`'`) на `inspection.html` и `ppe.html` (тот же паттерн в `value=`), запасной текст
  бейджа обёрнут в `esc`. `emergency.html` не трогал — там `esc` только в текстовых узлах. Рендер сверен
  (нет двойного экранирования), мобайл не задет.

---
