# Сценарий демо — SDS Safety

Сквозной показ за ~7 минут: от запроса оператора до аварийного реагирования.
Сценарий построен вокруг одного вещества — **хлора** (классический АХОВ, есть на водоканалах,
ЦБК, химзаводах) — чтобы провести зрителя через все слои продукта на одном примере.

## Подготовка (1 команда)

```bash
cd engine
CORPUS_FILE=../data/corpus_full_clean.json EMB_FILE=../data/embeddings_clean.npy \
  IDS_FILE=../data/embed_ids_clean.json SUBS_FILE=substances_clean.json \
  RETRIEVER=semantic LLM_BACKEND=ollama OLLAMA_LLM=qwen2.5:7b \
  uvicorn service:app --port 8000
```
Открыть **`home.html`** (отдаётся самим сервисом, напр. `http://127.0.0.1:8000/`). Адрес сервиса
определяется автоматически из `location.origin` — никаких полей вводить не нужно; индикатор
«система на связи» станет зелёным.

---

## Акт 1. Оператор спрашивает (console.html)

**Говорим:** «Оператор не листает 40-страничный паспорт — он спрашивает на естественном языке».

1. Режим **«Спросить»**, запрос: `какие СИЗ нужны при работе с хлором`
   → grounded-ответ qwen2.5 с цитатами разделов паспорта (не выдумка — модель отвечает по найденным фрагментам).
2. Режим **«Поиск»**, запрос: `первая помощь при отравлении хлором`
   → top-1 **хлор**, score ~0.77, карточки с разделом, score-баром и цитатой ГОСТ-формата.

**Аргумент:** семантика держит **top-1 94%** на чистой базе (47 вопросов), на тысячах веществ
лексический поиск проседает до 60% — поэтому прод-путь это эмбеддинги bge-m3, локально.

---

## Акт 2. Безопасность хранения (safety.html → вкладка «Совместимость»)

**Говорим:** «По данным Chemical Safety Board несовместимое хранение — фактор в ~25% химинцидентов».

- А: `хлор`, Б: `аммиак` → вердикт **осторожно** (два реактивных газа, разносить).
- А: `гипохлорит натрия`, Б: `соляная кислота` → вердикт **НЕЛЬЗЯ ВМЕСТЕ**, причина:
  *выделение газообразного хлора (Cl₂)* + цитаты Раздела 10 обоих паспортов.

**Аргумент:** вердикт не выдуман — это сегрегационные группы (NFPA/CSB) ПЛЮС текст Раздела 10
из наших паспортов. Инструмент отвечает не *да/нет*, а *почему*.

---

## Акт 3. Маркировка GHS (safety.html → вкладка «GHS»)

- Вещество: `хлор` → 5 пиктограмм СГС (ромбы): острая токсичность, окислитель, газ под давлением,
  раздражитель, опасность для среды + сигнальное слово **Опасно**.

**Аргумент:** пиктограммы выведены из меток паспорта и класса ГОСТ 12.1.007 — H-коды не выдумываем.

---

## Акт 4. Авария: расчёт зоны (safety.html → вкладка «Зона АХОВ»)

**Говорим:** «Произошёл выброс. Какая глубина зоны заражения?»

- АХОВ: `хлор`, масса: `40` т, ветер: `1` м/с, температура: `0` °C, устойчивость: `изотермия`.
- Результат: **Г ≈ 15 км** + раскладка: первичное облако Qэ1, вторичное Qэ2, время испарения,
  все коэффициенты K1–K7, токсодоза.

**Аргумент:** это не «AI придумал число» — расчёт по **РД 52.04.253-90** (методика ГО), коэффициенты
**верифицированы по 3 независимым копиям документа** (выловлены 2 опечатки в КонсультантПлюс).
Помечено `needs_review` — прогноз по типовой методике, не замена утверждённого расчёта объекта.
Вещества вне перечня 34 АХОВ РД (диборан, BF₃) система **честно отклоняет**, а не выдаёт фейк.

---

## Акт 5. Карточка реагирования (emergency.html)

- Вещество: `хлор` (или из АХОВ-перечня — `фосген`, `аммиак`).
- Карточка: признак АХОВ, ООН-номер, класс ДОПОГ, первая помощь, СИЗ — для спасателя в первые минуты.

**Аргумент:** один экран, крупно, для аварийной ситуации. Severity-полоса (красная = АХОВ).

---

## Акт 6. Честность как фича (safety.html → вкладка «Качество базы»)

**Говорим:** «Главное в безопасность-критичном инструменте — знать, чему можно доверять».

- Дэшборд: полны **1.7% паспортов**; **2546 — needs_review** и без СИЗ/первой помощи/хранения;
  бары дыр в критичных полях; аудит-лог запросов (комплаенс).

**Аргумент:** инструмент **не притворяется**, что все 2600 паспортов готовы. Он показывает, какие
дозаполнять перед оперативным применением. Приоритетные АХОВ (хлор, сероводород, SO₂, HCl, ТДИ)
уже обогащены верифицированными данными с источниками. Цена ошибки = здоровье → честность не опция.

---

## Закрытие

**On-prem под 152-ФЗ / КИИ:** Ollama на железе завода, данные не покидают контур, вендор-нейтральные
адаптеры (Ollama / OpenRouter / GigaChat). Один сервис — все слои: поиск, ответ, совместимость,
GHS, зоны, аварийные карточки, аудит. История работ и метрики — в `WORKLOG.md`.

| Слой | Эндпоинт | Обоснование |
|---|---|---|
| Поиск/ответ | `/search` `/ask` | top-1 94%, grounded, цитаты |
| Совместимость | `/compat` | NFPA/CSB + Раздел 10 |
| GHS | `/ghs` | СГС из паспорта |
| Зона АХОВ | `/zone` | РД 52.04.253-90 (верифиц.) |
| Авария | `/emergency` | ООН/ДОПОГ/первая помощь |
| Гигиена | `/quality` `/audit` | полнота + комплаенс |
