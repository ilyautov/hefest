fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
scikit-learn>=1.4
rank-bm25>=0.2.2
numpy>=1.26
scipy>=1.11
httpx>=0.27
pdfplumber
beautifulsoup4
lxml
segno>=1.6                # QR-коды (чистый python, без C-зависимостей) для этикеток/карточек

# --- семантический стек (прод on-prem, опц.) ---
qdrant-client>=1.10        # векторная БД (локальный режим или Docker-сервер)
# реранкер (кросс-энкодер bge-reranker-v2-m3): torch + transformers
torch>=2.2                 # MPS на Apple Silicon / CUDA
transformers>=4.40
# OCR грязных PDF (Ступень-1, аудит-спринт)
pymupdf>=1.24             # растеризация PDF без системного poppler
easyocr>=1.7              # OCR ru+en на torch/MPS, on-prem
# ВНИМАНИЕ: sentence-transformers НЕ использовать — тянет torchcodec->ffmpeg (libavutil);
# реранкер загружается напрямую через transformers (см. reranker_model.py).
